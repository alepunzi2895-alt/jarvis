## 09:59 — fix: dashboard non parlava mai le risposte

- Fatto: segnalato "la trascrizione prende i comandi ma non sento la
  risposta parlata, solo scritta in console". Trovato che `core/web_bridge.py`
  (poller dashboard) non chiamava mai il TTS locale — solo `bot.py`
  (Telegram) lo faceva via `speak_locally()`. Il mic della dashboard
  (fix di ieri, trascrizione locale) non era mai collegato all'uscita
  vocale. Estratto motore/pulizia markdown/interruttore `/voce on|off` in
  `core/voice/tts.py::speak_if_enabled()` (nuovo), riusato sia da
  `bot.py::speak_locally` (ora solo un alias) sia da `web_bridge.py`
  (chiamato dopo intent rapido e dopo `run_claude`, stesso punto esatto
  di bot.py). Branch `fix/dashboard-tts-response`, mergiato e pushato
  su main.
- Esito: ok (117/117 test verdi). Non verificato dal vivo — serve
  riavvio del bridge locale (`bot.py`) per caricare il nuovo codice.
- Imparato: qualsiasi nuovo canale di input va controllato anche sul lato
  "risposta" — il bridge web ha sempre avuto solo metà del giro voce
  (STT sì, TTS no) perché `speak_locally` è nato pensato solo per
  Telegram e non è mai stato esteso.
- Prossimo passo: chiedere a lui di riavviare il bridge locale e provare
  un comando vocale reale dalla dashboard per confermare che ora sente
  la risposta.

## 10:16 — feature: ascolto a mani libere sulla dashboard (parola d'attivazione "Jarvis")

- Fatto: chiesto "non c'è un modo per parlarci senza cliccare ogni volta il
  microfono?". Proposto hotkey vs hands-free vero, scelto hands-free.
  Riscritto `setupVoice()` in `web/public/app.js`: un click arma
  l'ascolto e resta acceso finché non lo spegni tu, ogni segmento
  parte/si ferma da solo (stesso rilevamento voce/silenzio di prima, ora
  in loop). Aggiunto filtro server-side obbligatorio in
  `core/web_bridge.py::_strip_wake_word()` — un segmento diventa un task
  reale solo se contiene "Jarvis", altrimenti torna con status "ignored"
  e la console lo rimuove senza mostrarlo. Stesso identico problema e
  stessa soluzione già visti una volta con l'ascolto continuo del vecchio
  riconoscimento cloud del browser (log 2026-07-14: ~230 task spuri,
  ~1.33$ di chiamate Claude vere prima che esistesse un filtro). Nuovo
  `tests/test_web_bridge.py` (6 test sul filtro). Mergiato e pushato su
  main, 123/123 test verdi.
- Esito: ok (test/sintassi), **non verificato dal vivo** — richiede
  riavvio del bridge locale + un test reale col microfono.
- Imparato: la trascrizione ora avviene lato server (faster-whisper nel
  bridge locale), non più nel browser come ai tempi della Web Speech
  API — quindi anche il filtro "Jarvis" si è spostato lato server
  (`core/web_bridge.py`) invece che in `app.js` come nella versione
  precedente di questa stessa idea.
- Limite noto, non verificato: il segmento inizia a registrare solo
  DOPO che il volume supera la soglia (nessun pre-buffer/lead-in) — la
  primissima sillaba di "Jarvis" può risultare tagliata e far fallire il
  riconoscimento della wake word. Da controllare dal vivo; se è un
  problema reale serve un piccolo buffer circolare pre-soglia (non fatto
  oggi, per tenere il diff minimo).
- Prossimo passo: riavviare bot.py (stessa procedura di Task
  Manager + Utilità di pianificazione già descritta oggi) e provare
  "Jarvis, [comando]" a voce dalla dashboard, verificando sia che parta
  da solo il segmento sia che il filtro lasci passare comandi veri e
  scarti il rumore di fondo.

## 10:40 — fix: latenza risposte + specchio Telegram + ricognizione progetti Downloads

- Fatto: tre richieste in un colpo solo.
  1. **Latenza**: `core/claude_bridge.py`/`core/claude_api.py` chiamavano
     meteo e second brain (Turso) in sequenza prima di ogni `claude -p` —
     ora in parallelo (`asyncio.create_task` + due `await`). Timeout Turso
     20s → 6s, configurabile (`JARVIS_TURSO_TIMEOUT_SEC`): un blip di rete
     ora degrada in fretta invece di bloccare l'intera risposta per 20s.
     Il floor del processo CLI stesso (~11-12s, gia' misurato in passato)
     resta — risolverlo del tutto richiederebbe passare all'API diretta
     come la voce, perdendo Read/Write/Bash reali (scelta esplicita gia'
     presa il 2026-07-15 di non farlo per testo/dashboard). Segnalato a
     lui invece di deciderlo da solo.
  2. **Specchio Telegram**: ogni esito della dashboard (intent rapido o
     `run_claude`) ora arriva anche su Telegram, stesso schema gia'
     esistente per la voce (`core/voice/daemon.py::_notify_telegram`).
     Nuovo `core/web_bridge.py::_notify_telegram()`.
  3. **Ricognizione progetti**: letti README/CLAUDE.md/PROJECT_DIRECTIVES
     + `git log`/`status` di VMScout, Property Scout, ConciergeFlow,
     WhiteSoul Ibiza, AP Systems (portfolio) e TradeFlow AI sotto
     `Downloads/`. Creati `memory/projects/vmscout.md`,
     `property-scout.md`, `conciergeflow.md`, `whitesoulibiza.md`,
     `alessandropunzi-systems.md`. Aggiornato `tradeflow-ai.md`: la
     memoria era ferma al 2026-07-16 ("bot fermo") — nel codice quel bug
     risulta gia' risolto (fix 2026-09-02 e 2026-09-09), ma **non
     verificabile da qui se il riavvio bot richiesto sulla VPS sia
     davvero stato fatto**.
  Mergiato e pushato tutto su main (codice), 123/123 test verdi.
- Esito: ok (codice), parziale (ricognizione: buona ma non esaustiva,
  vedi sotto).
- Imparato: **trovata un'anomalia reale in ConciergeFlow** — un secondo
  repo Git annidato dentro il primo
  (`Downloads/ConciergeFlow/ConciergeFlow/.git`), stesso remote GitHub,
  HEAD piu' recente (giugno) e allineato a `origin/main`, mentre l'esterno
  e' un clone stale di aprile mai piu' toccato. Nessun rischio di perdita
  dati (verificato che l'interno combacia con origin/main) ma
  `JARVIS_ALLOWED_DIRS` punta all'esterno/stale — se in futuro JARVIS
  legge lo stato git di quel path, legge quello sbagliato. **Non toccato**,
  segnalato solo in memoria: e' una scelta sua se e come ripulirlo.
  Anche **cron giornaliero di TradeFlow AI ancora fermo da luglio**,
  scollegato dallo sviluppo del codice (molto attivo) — probabile
  scheduled task VPS mai ripristinato.
- Prossimo passo: chiedere ad Alessandro (a) se il riavvio bot VPS di
  TradeFlow dopo i fix di settembre e' stato fatto, (b) cosa vuole fare
  della cartella ConciergeFlow annidata, (c) se vuole che gli altri
  progetti "minori" (VMScout/Property Scout/ConciergeFlow) entrino anche
  loro in `/progetti` (oggi solo Aura/TradeFlow/WhiteSoul).

## 11:08 — verifica integrazione Outlook COM + fix crash reale trovato

- Fatto: chiesto di "montare" l'integrazione COM per leggere Outlook
  desktop — esisteva gia' (`core/outlook.py`, costruita 2026-09-14), quindi
  invece di ricostruirla l'ho testata dal vivo per confermare che sia
  ancora mantata e funzionante. Prima esecuzione: **crash reale**,
  `UnicodeEncodeError` su un carattere Unicode fuori dalla codepage
  Windows (spazio tipografico in una mail HTML) durante il `print()` del
  risultato. Causa: `bot.py` (processo che ospita sia il bot Telegram sia
  il poller web, stesso identico processo) non ha mai avuto il fix
  `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` gia'
  applicato una volta a `core/voice/daemon.py` per lo stesso identico
  problema (allora sulla trascrizione vocale, ora su Outlook — stessa
  causa radice, contenitore diverso). Aggiunto anche qui. Ritestato: 5
  mail lette correttamente (Iveco/Databricks/Qlik, contenuto reale).
  123/123 test verdi. Mergiato e pushato su main.
- Esito: ok — integrazione confermata funzionante, bug reale trovato e
  risolto nello stesso giro.
- Imparato: quando un processo Windows reindirizza stdout su file (come fa
  il wrapper `.vbs` dell'autostart), il fix `stdout.reconfigure` va
  applicato **per ogni entry point separato** (bot.py/daemon.py sono
  processi diversi, ognuno con la propria init) — non basta averlo fatto
  una volta in un punto del codice. Vale la pena controllare se
  `core/web_bridge.py`, se mai eseguito standalone fuori da bot.py, ne
  abbia bisogno anche lui (oggi gira sempre dentro il processo di bot.py,
  quindi e' gia' coperto).
- Prossimo passo: nessuno bloccante — l'integrazione Outlook e' pronta
  all'uso ("controlla la posta" da Telegram/voce/dashboard).

## Intent Outlook: la posta non veniva mai letta

**Task** — Alessandro chiede tre volte di leggergli l'ultima mail; io rispondo
che l'integrazione non esiste. Sbagliato: `core/outlook.py` c'e' da tempo.

**Esito** — Due problemi distinti, uno risolto:

1. *Il bug vero (risolto)* — `OUTLOOK_INTENT_RE` non matchava nessuna delle sue
   formulazioni parlate: "mi leggi l'ultima mail", "puoi leggermi l'ultima
   mail", "mi controlli la posta?". La vecchia regex voleva il verbo esatto e il
   sostantivo attaccato. Allargata a radice-verbo + riempimento + sostantivo.
   11 varianti reali passano, 5 falsi positivi restano fuori. Merge su main in
   locale, **non pushato**.

2. *Non verificabile da qui* — la lettura COM vera fallisce nella mia shell con
   `CO_E_SERVER_EXEC_FAILURE` / `MK_E_UNAVAILABLE` perche' la shell gira
   **elevata (admin)** mentre Outlook gira come utente normale, e COM rifiuta
   l'attivazione tra livelli di integrita' diversi. Non e' un difetto del
   modulo. Va riprovato dal bridge JARVIS normale (non elevato).

**Cosa ho imparato**

- Prima di dire "non ce l'ho", **guardare `ls core/`**. Ho affermato due volte
  che l'integrazione email non esisteva mentre il modulo era la' dentro, e ho
  perfino proposto ad Alessandro di costruirla da zero. Grep sul repo intero
  annegato nei risultati di `.venv/` — restringere sempre alle cartelle del
  progetto.
- Una feature con l'intent rotto e' indistinguibile da una feature assente, dal
  lato utente. Quando una capacita' "non risponde", sospettare il router di
  intent prima di concludere che manchi il codice.
- Test COM da una shell elevata: mai conclusivi. Il livello di integrita' del
  processo chiamante deve combaciare con quello del client Office.

## 11:29 — feature: apertura Databricks QAS via browser + lettura schermo per Genie Code

- Fatto: chiesto di aprire Databricks di test navigandolo davvero, poi
  leggere l'ultima risposta di Genie Code e infine dettare un nuovo input.
  Testato dal vivo aprendo `DATABRICKS_QAS_HOST` nel browser persistente
  di JARVIS (`core/browser.py`): naviga per davvero, si ferma alla
  schermata di login SSO — stesso comportamento gia' visto con Teams,
  serve un suo login manuale una tantum in quella finestra prima che
  JARVIS possa vedere altro. **Nota**: e' emerso durante questo test che
  un'altra sessione JARVIS (bot live, co-authored Claude Opus 5) aveva
  gia' commitato in locale (non pushato) un fix reale della regex intent
  Outlook nello stesso momento — pushato subito, nessun conflitto, solo
  file diversi.
  Aggiunto: (1) l'URL QAS nel SYSTEM prompt (`core/claude_bridge.py`) cosi'
  "apri Databricks" usa il blocco ```browser``` gia' esistente, zero
  meccanismo nuovo; (2) "genie" tra i trigger di
  `core/screen_context.py::_SCREEN_RE`, cosi' "leggimi la risposta di
  Genie Code" allega davvero uno screenshot del desktop (stesso
  meccanismo di Teams/schermo). 124/124 test verdi (1 nuovo). Mergiato e
  pushato su main (un 500 transitorio di GitHub al primo tentativo di
  push, riuscito al secondo).
- Esito: parziale. Apertura Databricks e lettura schermo: ok. Digitare
  input dentro Genie Code: **non costruito** — richiede un'azione
  Playwright mirata su un DOM che non ho mai visto (il browser di JARVIS
  si e' fermato al login SSO), e `core/browser.py` esclude
  deliberatamente click/fill generico per sicurezza. Non ho tirato a
  indovinare selettori su una SPA aziendale complessa.
- Imparato: per costruire l'ultimo pezzo (scrivere dentro il pannello
  Genie Code) serve prima che Alessandro faccia il login SSO nella
  finestra di JARVIS gia' aperta e navighi fino al notebook/conversazione
  Genie Code che gli interessa, cosi' una sessione futura puo' ispezionare
  la UI reale invece di indovinare.
- Prossimo passo: chiedere ad Alessandro di completare il login SSO nella
  finestra del browser di JARVIS (gia' aperta su questa macchina) e
  indicare dove si trova la conversazione Genie Code da leggere/scrivere,
  cosi' si puo' costruire l'azione di input mirata sul DOM vero.

## 11:39 — feature: dashboard più futuristica + bolla reattiva al parlato + mic sempre attivo

- Fatto: chiesto (1) dashboard più futuristica, con un'immagine di
  riferimento (HUD stile Iron Man/Stark Industries, tema Rainmeter blu/
  ciano — scaricata e vista in `.tmp/design_ref.jpg`); (2) la bolla
  centrale deve muoversi/cambiare colore quando JARVIS parla; (3) niente
  più click per armare/disarmare il microfono. Fatte tutte e tre, palette
  invariata (accento blu fisso, mai toccato senza chiedere — vedi
  [[feedback-dashboard-accent-color]]):
  - CSS additivo: due anelli radar giganti dietro la bolla, scanline
    sottile su tutta la pagina, pannelli con angolo tagliato + linea
    luminosa sotto gli header (`web/public/style.css`).
  - Bolla reattiva: nuova tabella `runtime_flags` su Turso,
    `core/voice/tts.py` scrive `speaking=1/0` intorno alla riproduzione
    audio vera (sia `speak()` sia `speak_stream()`, fire-and-forget in
    thread — non deve mai rallentare la voce), `web/api/jarvis.js` action
    `runtime_status` la legge, `app.js` fa polling ogni 700ms e aggiunge
    `.orb-speaking` (movimento più rapido + pulsare verde invece del
    respiro blu). Funziona per QUALSIASI canale che fa parlare JARVIS
    (Telegram/voce/dashboard), non solo quello che ha innescato la
    risposta — è il primo punto dove la dashboard "vede" lo stato del
    daemon vocale nativo.
  - Mic sempre attivo: `setupVoice()` ora arma l'ascolto a mani libere
    subito al boot (richiede comunque il permesso nativo del browser la
    prima volta), il click resta solo come interruttore manuale.
  129/129 test verdi (5 nuovi su `core/voice/tts.py`). Mergiato e pushato
  su main.
- Esito: ok (codice/test), **non verificato dal vivo** — richiede
  riavvio del bridge locale + aprire davvero la dashboard nel browser per
  vedere l'effetto visivo e provare mic/bolla per davvero.
- Imparato: l'audio di JARVIS esce sempre dagli altoparlanti del PC via
  un processo Python separato (mai nel browser) — qualunque feature
  futura che voglia far "sapere" alla dashboard cosa sta facendo la voce
  deve per forza passare da uno stato condiviso (qui Turso), non può mai
  ispezionare direttamente lo stato audio del processo.
- Prossimo passo: aprire la dashboard vera nel browser e verificare (a)
  il nuovo look, (b) che il mic parta da solo senza click, (c) che la
  bolla cambi davvero aspetto durante una risposta parlata reale.

## 11:51 — fix: retry automatico su errori COM transitori di Outlook

- Fatto: segnalato che "leggimi l'ultima mail" tornava un errore reale —
  HRESULT 0x80080005 (CO_E_SERVER_EXEC_FAILURE, "Server execution
  failed"). Ricontrollato dal vivo la stessa identica chiamata (nessuna
  modifica di configurazione nel mezzo): funzionava. Confermato
  transitorio — Outlook desktop via COM e' notoriamente soggetto a blip
  quando e' momentaneamente occupato. Aggiunto retry automatico in
  `core/outlook.py::_fetch_recent_sync()`: fino a 3 tentativi, 1.5s di
  pausa tra uno e l'altro, prima di restituire l'errore. 129/129 test
  verdi. Mergiato e pushato su main.
- Esito: ok. Non un problema di configurazione ne' dell'integrazione (era
  gia' confermato funzionante ieri e di nuovo oggi mattina) — solo
  mancava resilienza a un blip normale per questo tipo di automazione.
- Imparato: l'integrazione Outlook COM va trattata come intrinsecamente
  soggetta a blip transitori (stesso principio gia' applicato a Turso/
  rete in altre parti del codice) — il retry qui e' lo stesso pattern,
  solo per un sottosistema diverso (COM locale invece di rete).
- Prossimo passo: nessuno bloccante. Se il problema si ripresenta con
  frequenza anche dopo il retry, andra' controllato se Outlook ha un
  dialogo bloccato in primo piano (es. richiesta di credenziali, avviso
  di sicurezza) che nessuno sta chiudendo.

## 12:22 — feature grossa: testo/Telegram/dashboard passano all'API diretta

- Fatto: chiesto "cosa perdo passando all'API diretta" — spiegato il
  trade-off (Read/Write/Bash reali). Risposta: "per me va bene passiamo
  all'api se è più veloce e se serve implementiamo tutte le cose che
  servono". Usato Plan Mode data la portata (motore centrale di JARVIS).
  Verificato che il "quello che serve" era meno del temuto:
  `core/claude_bridge.py`/`core/claude_api.py` condividono gia' l'intera
  pipeline brain/browser/databricks/system_actions (pura estrazione di
  testo, non tool reali). L'unico pezzo mancante — Read/Write/Bash — ha
  gia' un sostituto pronto e piu' sicuro: `core/system_executor.py`
  (whitelist + conferma via `/confirm` gia' costruiti). Confermato con
  lui: modello **Sonnet 5**, whitelist git ampliata (checkout/switch/
  merge automatici, **push resta sempre dietro conferma**).
  Costruito `_run_claude_api()` in `core/claude_bridge.py`: loop agentic
  manuale (non Tool Runner beta) con 4 tool (read_file/write_file/
  list_dir/run_command) appoggiati a SystemExecutor. `run_claude()` ora
  e' un dispatcher: workspace "trading" resta su `claude -p` (serve MCP
  TradingView, non raggiungibile dall'API diretta), tutto il resto va
  al nuovo motore. Ripiego manuale `JARVIS_TEXT_ENGINE=cli` in `.env` se
  servisse tornare indietro senza toccare codice. History persistita in
  `state.json` sotto una chiave nuova (`api_sessions`, testo pulito, non
  piu' il `session_id` opaco del `--resume` CLI).
  151/151 test verdi (25 nuovi: `tests/test_claude_bridge.py` +
  ampliamento `tests/test_system_executor.py`). **Verificato dal vivo**
  (non solo test): `read_file` reale su `memory/profile.md` -> risposta
  corretta, $0.026; una domanda semplice ("che ore sono e che tempo fa")
  risposta in ~1-2s di latenza reale della chiamata (contro l'11-12s+ del
  processo CLI). Piano completo in
  `C:\Users\f45038c\.claude\plans\splendid-herding-sutherland.md`.
  Mergiato e pushato su main.
- Esito: ok — cambio grosso, verificato sia a unit test sia dal vivo con
  chiamate reali (non solo mock).
- Imparato: i due motori (CLI/API) condividevano gia' molta piu' logica
  di quanto sembrasse dal solo leggere i docstring — la vera differenza
  era ristretta a un'unica cosa (tool reali), e per quella esisteva gia'
  un sostituto pronto (SystemExecutor) mai collegato prima a un motore
  Claude generico, solo a `/run`/`​```system```` e ai comandi rapidi.
- Non ancora verificato dal vivo: un vero task da Telegram/dashboard reale
  (non uno script di test), un flusso git completo guidato da Claude
  (branch->modifica->commit->merge in locale, verificare che un eventuale
  push chieda comunque conferma), il comportamento quando l'API restituisce
  un errore reale (rate limit/auth) — oggi si affida al catch-all generico
  gia' esistente in bot.py/web_bridge.py, non testato con un errore vero.
- Prossimo passo: chiedere ad Alessandro di provarlo per davvero da
  Telegram/dashboard (richiede il solito riavvio del bridge locale) e
  dirmi se i tempi sono davvero migliorati e se il comportamento gli
  sembra corretto.

## 12:37 — fix: mic dashboard trascriveva vuoto + conteggio mail non lette

- Fatto: segnalato "non ho capito niente dall'audio" ripetuto, con
  richiesta di eseguire subito "leggi l'ultima mail"/"quante mail non
  lette"/"chat Teams non lette". Causa quasi certa della trascrizione
  vuota: la cattura a mani libere di stamattina creava un
  `MediaRecorder` nuovo solo DOPO il rilevamento voce (nessun
  pre-buffer, gia' segnalato come limite noto ma sottovalutato — tagliava
  probabilmente piu' della sola prima sillaba). Riscritta la cattura con
  `AudioContext`/`ScriptProcessorNode`: PCM grezzo continuo, buffer
  circolare ~400ms sempre pronto PRIMA del rilevamento, incapsulato in
  un WAV al volo lato client (non piu' webm/opus via MediaRecorder).
  Verificato l'intero giro con una frase sintetizzata reale (edge-tts ->
  WAV client-side replicato in Python -> PyAV -> faster-whisper):
  decodifica e trascrizione corrette. **Trovato nello stesso test**:
  whisper in italiano trascrive "Jarvis" foneticamente in modo diverso
  (osservato "YARVIS") — il filtro parola d'attivazione richiedeva la
  grafia esatta, allargato a un nucleo fonetico comune
  (core/web_bridge.py::_WAKE_WORD_RE). Aggiunto anche conteggio dedicato
  mail non lette (`core/outlook.py::count_unread_emails*`, usa
  `MAPIFolder.UnReadItemCount` invece di una lista troncata a 8 che
  avrebbe dato un numero sbagliato per difetto) — "quante mail ho non
  lette?" ora risponde con un conteggio vero, verificato dal vivo contro
  la sua Inbox reale ("Nessuna mail non letta" — corretto). 163/163 test
  verdi (24 nuovi). Mergiato e pushato su main.
- Esito: ok su mail/wake-word (verificato con test mirati, non solo
  unit test). **Non verificato dal vivo il fix del microfono** — la
  sintesi vocale di prova non e' la sua voce reale, serve una prova sua
  dopo il riavvio del bridge per confermare che risolva davvero.
- Non affrontato: "chat Teams non lette". Oggi Teams e' solo screenshot
  + lettura visiva di Claude (core/screen_context.py), niente lista
  "non lette" strutturata — servirebbe Microsoft Graph API (OAuth app
  registration nuova) per farlo per davvero, non un piccolo fix. Non
  costruito, da chiedere se lo vuole davvero prima di partire.
- Imparato: un limite "noto ma non verificato dal vivo" segnalato a fine
  di una sessione precedente (qui: niente pre-buffer) va trattato come
  probabile causa quando arriva un bug nella stessa area, non solo come
  nota a margine — avrei potuto sospettarlo subito invece di generare
  teorie alternative.
- Prossimo passo: chiedere ad Alessandro di riprovare dal vivo dopo il
  riavvio del bridge, e se vuole che si costruisca l'integrazione Teams
  vera (Graph API) per "chat non lette".

## 12:51 — bug mic dashboard confermato reale (non solo processo vecchio) + logs a 0 byte

- Fatto: Alessandro ha riavviato il bridge (confermato: nuovo processo
  dalle 12:43, dopo tutti i fix di oggi) e ha ripreso lo stesso identico
  errore ("non ho capito niente dall'audio") — quindi il fix del mic di
  stamattina (pre-buffer PCM/WAV) NON risolve da solo, il problema e'
  piu' profondo di quanto pensassi. Provato a leggere `logs/bot.log` per
  capire cosa arriva davvero dal browser: **0 byte** dopo 15+ minuti di
  processo attivo e più tentativi già falliti. Causa: un file (non un
  terminale) usa di default un buffer a blocchi — `print()` restava
  invisibile finche' il buffer non si riempiva o il processo terminava.
  Aggiunto `line_buffering=True` a `sys.stdout`/`stderr.reconfigure()` in
  `bot.py` e `core/voice/daemon.py` (stesso fix in entrambi). Aggiunta
  diagnostica temporanea in `core/web_bridge.py::_transcribe_audio()`:
  logga bytes ricevuti + durata WAV decodificata + testo trascritto
  (anche vuoto) — prima nessuna traccia di COSA arrivava dal browser.
  163/163 test verdi. Mergiato e pushato su main.
- Esito: parziale/bloccato. Il fix di stamattina non basta da solo; senza
  log leggibili non potevo nemmeno vedere SE arrivava audio vero, quanto
  durava, o se whisper lo giudicava silenzio — ora al prossimo tentativo
  ci sara' evidenza concreta invece di ipotesi.
- Domanda di Alessandro: "perche' fa la trascrizione? non dovrebbe
  ascoltare diretto dal microfono?" — risposto che una qualche forma di
  riconoscimento vocale (trascrizione) e' l'UNICO modo per trasformare
  audio in testo comandabile; non esiste un "ascolto diretto" che la
  aggiri. Il daemon vocale nativo (hey jarvis/hotkey) ha in piu' un
  modello di wake-word dedicato PRIMA della trascrizione vera (piu'
  leggero, reagisce subito); la dashboard invece fa VAD locale (rileva
  solo "c'e' un suono", non riconosce la parola) e poi trascrive TUTTO
  con whisper per controllare se contiene "Jarvis" — architetturalmente
  diverso, non ha un passaggio "diretto" equivalente da saltare.
- Imparato: **controllare SEMPRE che i log siano davvero scritti prima**
  di fidarsi della loro assenza di errori — un log a 0 byte non significa
  "nessun problema", puo' semplicemente significare "buffering". Vale la
  pena controllare questo stesso pattern (line_buffering) anche altrove
  se in futuro un log sembra sospettosamente vuoto.
- Prossimo passo (bloccante): chiedere ad Alessandro di riavviare ANCORA
  una volta il bridge (per il fix di logging) e riprovare un comando
  vocale dalla dashboard, poi leggere `logs/bot.log` per vedere la riga
  diagnostica `[web audio] ... bytes, ...ms -> trascritto: ...` e capire
  finalmente cosa arriva davvero.

## 15:09 — trovato il bug vero del mic (rumore -> allucinazioni whisper) + credito Anthropic esaurito

- Fatto: letto `logs/bot.log` dopo il riavvio (finalmente popolato, fix
  del logging funzionante). Trovato il bug reale: la soglia di
  rilevamento voce (0.02 su scala float -1..1) scattava su rumore di
  fondo/ventola, restava "in ascolto" fino al tetto di 15s ripetutamente
  (4 segmenti nei log, tutti esattamente a 15019ms), e whisper
  **allucinava** testo plausibile su quell'audio quasi silenzioso —
  frasi come "Sottotitoli e revisione a cura di QTSS" ripetute piu'
  volte, un artefatto ben noto di whisper su input silenzioso/rumoroso
  (confonde silenzio/rumore con sottotitoli di film, non e' un bug di
  decodifica). Il fix del pre-buffer/WAV di stamattina era corretto ma
  non bastava da solo. **Confermato pero' che il giro funziona quando un
  segmento e' pulito**: "Hey Jarvis, che ora sono?" e' stato catturato,
  trascritto e processato correttamente (`> [web] che ora sono?` nel
  log).
  Fix: soglia alzata 0.02 → 0.06 + debounce 200ms continui prima di
  committare l'inizio registrazione (un blip isolato non basta più);
  un segmento che tocca il tetto di 15s senza MAI una pausa vera viene
  scartato invece di essere trascritto (nei log reali era sempre rumore,
  mai un comando vero). 163/163 test verdi. Mergiato e pushato su main.
  **Non ancora riverificato dal vivo con questa soglia nuova.**
- **Bloccante separato e piu' urgente, segnalato da Alessandro a metà**:
  `Error code: 400 ... Your credit balance is too low to access the
  Anthropic API`. Il motore API di oggi (testo/Telegram/dashboard) e la
  voce (gia' da luglio) fatturano entrambi su `ANTHROPIC_API_KEY` —
  il credito si e' esaurito, probabilmente per il volume di chiamate
  reali di oggi (mio testing + suoi tentativi + sessione bot live
  concorrente). **Nessuna azione mia possibile** — deve andare su
  console.anthropic.com → Plans & Billing e aggiungere credito. Finche'
  non lo fa, TUTTO il testo/Telegram/dashboard fallisce con questo
  stesso errore (non e' un bug, e' il conto vuoto). Ripiego temporaneo
  possibile se non vuole aggiungere credito subito: `JARVIS_CLI_BARE=0`
  in `.env` torna a autenticazione abbonamento/OAuth invece che a
  consumo sull'API key (`JARVIS_TEXT_ENGINE=cli` da solo NON basta,
  `--bare` fattura comunque sull'API key).
- Imparato: whisper su audio silenzioso/rumoroso non dice "non sento
  niente" — inventa testo plausibile con sicurezza. Un segmento che
  tocca sempre il tetto massimo di durata senza mai una pausa e' un
  segnale forte che e' rumore di fondo, non un comando reale — utile
  euristica generale per qualunque VAD futuro in questo repo.
- Prossimo passo: (1) Alessandro deve aggiungere credito Anthropic (o
  attivare il ripiego `JARVIS_CLI_BARE=0`) prima che qualunque test
  ulteriore abbia senso; (2) dopo, riavviare ancora il bridge per la
  soglia VAD nuova e riprovare un comando vocale reale.

## 15:19 — credito ricaricato, trovata causa radice: whisper non riconosce "Jarvis"

- Fatto: Alessandro ha ricaricato il credito Anthropic. Verificato che il
  fix VAD di prima fosse gia' live su Vercel (fetch diretto di
  `jarvis-dashboard-green.vercel.app/app.js`: si', soglia 0.06 gia'
  pubblicata) — quindi il "non recepisce" segnalato dopo non era ne' il
  credito ne' il VAD, serviva solo un hard refresh del browser (cache).
  Guardando `logs/bot.log` dopo il suo nuovo tentativo, trovata la causa
  radice vera: whisper in italiano trascrive "Jarvis" (nome inventato)
  in modo incoerente — un tentativo ha dato "Che ora è?" **senza alcuna
  parola d'attivazione** (persa del tutto, non solo storpiata), un altro
  "Giorbis chiarai" (variante fonetica non coperta nemmeno dal regex
  allargato di stamattina). Inseguire varianti fonetiche via regex era
  whack-a-mole. Fix alla radice: `initial_prompt="Jarvis, il maggiordomo
  AI di Iron Man."` passato a `faster-whisper` (`core/voice/stt.py`) —
  polarizza il decoder verso la grafia corretta. Verificato con lo stesso
  file di test che prima dava "YARVIS": ora "Jarvis" esce giusto in ogni
  variante di prompt provata; verificato anche che NON introduce falsi
  positivi (frasi neutre restano neutre; su silenzio vero whisper
  allucina frammenti del prompt stesso, mai "Jarvis" isolato — il filtro
  wake-word lato server resta comunque come rete di sicurezza). 163/163
  test verdi. Mergiato e pushato su main.
- Esito: ok, alta fiducia — verificato con lo stesso identico caso che
  falliva prima, non solo in teoria. **Serve un altro riavvio del
  bridge** (questo e' lato Python/server, non lato browser come il VAD).
- Imparato: quando una parola-chiave inventata (nome del prodotto) deve
  essere riconosciuta da un motore STT generico, il modo giusto di
  risolverlo e' polarizzare il decoder (`initial_prompt`), non inseguire
  ogni possibile trascrizione errata con un regex sempre più largo — piu'
  robusto e risolve la causa, non i sintomi.
- Prossimo passo: Alessandro deve riavviare il bridge un'ultima volta
  (fix lato Python) e riprovare "Jarvis, che ora è" dal vivo.

## 15:25 — bug serio trovato: il mic si ri-sente da solo (feedback acustico) + isolamento Outlook COM

- Fatto: continuando a leggere `logs/bot.log`, trovato un bug
  architetturale mai notato prima: con l'ascolto sempre attivo di
  stamattina E le risposte lette ad alta voce dagli altoparlanti del PC
  (gia' esistente, ma mai attivo insieme all'ascolto sempre acceso fino
  ad oggi), **il microfono si risentiva da solo** — ritrascriveva le
  proprie risposte come un nuovo comando. Prova diretta nei log: un
  errore Outlook letto ad alta voce ("...dettaglio 2, 1, 4, 6, 9, 5, 9,
  3, 5, 5...") e' stato ricatturato dal mic e ritrascritto quasi
  identico subito dopo. L'echo cancellation del browser non lo previene:
  cancella solo audio che il BROWSER STESSO riproduce (WebRTC/`<audio>`),
  non un processo Python separato che scrive sugli altoparlanti
  (`core/voice/tts.py`, via `sounddevice`). Fix: `web/public/app.js`
  legge lo stesso flag "sta parlando" gia' costruito oggi per la bolla
  centrale (Turso) e zittisce completamente il mic mentre e' vero,
  scartando anche un segmento gia' in corso quando scatta. Questo
  probabilmente spiega buona parte della lentezza percepita: ogni
  auto-ascolto intasava la coda di trascrizione whisper (condivisa),
  ritardando i comandi veri in attesa dietro segmenti spazzatura
  autogenerati.
  Isolata anche ogni chiamata Outlook COM su un thread OS dedicato e mai
  riusato (`core/outlook.py::_run_in_fresh_thread`) invece del pool
  condiviso di `asyncio.to_thread` — sospetto plausibile (non confermato)
  per il fallimento 0x80080005 mai riprodotto isolando la stessa
  identica chiamata: `CoInitialize`/`CoUninitialize` sono per-thread, un
  thread riciclato da una chiamata precedente non centrata su Outlook
  puo' lasciare l'apartment STA sporco. 166/166 test verdi (16 nuovi).
  Mergiato e pushato su main. Verificato dal vivo: lettura reale
  (conteggio non lette + ultime mail) funziona ancora con il thread
  dedicato.
- Esito: ok per Outlook (verificato dal vivo di nuovo funzionante). Il
  fix del feedback acustico e' lato browser (Vercel, gia' live appena
  pushato) — **non richiede riavvio del bridge locale**, solo che la
  scheda del browser ricarichi `app.js` (succede da solo alla prossima
  apertura, o con hard refresh se la scheda e' gia' aperta).
- Imparato: due feature "sempre attive" costruite in momenti diversi
  (voce sempre in ascolto stamattina, risposte sempre parlate a
  meta' luglio) possono interagire male anche se ciascuna testata a
  parte sembra a posto — un mic sempre acceso + altoparlanti sempre
  attivi e' un feedback loop quasi garantito se non esplicitamente
  gestito. Da controllare sempre l'interazione tra feature "always-on"
  costruite separatamente, non solo ciascuna isolata.
- Prossimo passo: Alessandro deve riprovare dal vivo (hard refresh se la
  scheda era gia' aperta) e confermare se la lentezza percepita e'
  migliorata ora che il mic non si autoascolta piu'.

## 15:41 — intent rapidi ora/meteo (3.13s -> 0.008s/1.1s)

- Fatto: richiesta esplicita "riduciamo i tempi di domanda e risposta,
  max 4-5s". Misurato dal vivo che "che ora è"/"che tempo fa" passavano
  per l'intero motore API (~3.13s) anche se Claude rispondeva gia' bene
  da solo (data/ora/meteo gia' nel SYSTEM prompt, nessun tool
  necessario) — mancava solo un intent rapido dedicato, a differenza di
  apri/chiudi app, volume, posta. Aggiunti intent "time"/"weather"
  (core/intents.py) — rispondono da datetime.now()/weather.py senza mai
  toccare Claude. Misurato dopo: "che ora è" 0.008s, "che tempo fa" 1.1s
  a cache fredda (poi quasi zero). 170/170 test verdi. Mergiato e
  pushato su main.

## 16:00 — Vercel disabilitato per pagamento, poi risolto

- Fatto: dashboard rotta con "Connessione persa"/errori JSON su quasi
  tutto. Trovato con un test diretto (`curl -i .../api/jarvis`):
  `HTTP 402 Payment Required`, `X-Vercel-Error: DEPLOYMENT_DISABLED` —
  l'intero progetto Vercel disabilitato per un problema di fatturazione
  sul suo account (probabile causa: volume di traffico altissimo oggi,
  incluso il loop del mic prima del fix). Nulla che potessi risolvere io
  — segnalato di andare su vercel.com a sistemare il pagamento.
  Alessandro ha aggiornato il piano; riverificato con lo stesso test
  diretto: `200 OK`. Risolto.
  Nota metodologica: durante la verifica ho incontrato IO STESSO blip di
  rete verso `*.vercel.app` da questa macchina (gia' documentato,
  [[network-quirks-this-pc]]) — mi sono fermato dopo pochi tentativi
  invece di insistere, per non rischiare di aggravare un blocco
  anti-abuso reale (la memoria racconta che e' gia' successo).

## 16:40 — bug critico: deadlock del mic "sta parlando" + streaming voce Telegram/dashboard

- Fatto: segnalato "dopo la prima risposta non rimane più in ascolto".
  Trovato un bug reale di design: la scrittura `speaking=false` a fine
  parlato e' fire-and-forget (`core/voice/tts.py::_set_speaking_bg`) —
  se quella singola scrittura fallisce (blip di rete, e la sua rete era
  proprio instabile in quel momento), il flag resta bloccato su "vero"
  **per sempre**: mic zittito in eterno -> nessun comando nuovo puo'
  arrivare -> nessuna nuova risposta -> il flag non si aggiorna mai piu'
  (deadlock reale, non recuperabile da solo). Fix in
  `web/api/jarvis.js::runtimeStatus()`: il flag "vero" scade da solo
  dopo 20s se non rinnovato — self-healing, non richiede che la
  scrittura di reset arrivi mai.
  Nello stesso giro, implementato lo streaming voce per frase anche per
  Telegram/dashboard (`core/voice/tts.py::_speak_sync` ora usa
  `speak_stream()` invece di `speak()`) — stesso guadagno gia' sfruttato
  dal canale vocale nativo. Misurato dal vivo su una risposta di 3 frasi:
  prima audio dopo 5.28s (tutto insieme, prima) -> 1.9s (streaming, ora).
  173/173 test verdi. Mergiato e pushato su main — fix lato Vercel/JS,
  gia' live, nessun riavvio del bridge necessario.
- Esito: ok, entrambi verificati con misure dirette (non solo teoria).
- Chiesto da Alessandro: vuole che l'esperienza sia come la voce nativa
  di Claude (real-time, streaming bidirezionale). Spiegato che e'
  un'architettura fondamentalmente diversa (oggi: registra segmento
  intero -> carica -> trascrivi tutto -> Claude -> sintetizza -> scarica
  -> suona; voce nativa Claude: streaming audio bidirezionale continuo)
  — non raggiungibile con fix incrementali, richiederebbe una riscrittura
  vera. Budget realistico onesto dato: ~4-6s per intent rapidi (ora
  raggiunto), ~7-9s per una domanda vera a Claude.
- Prossimo passo: Alessandro deve riprovare dal vivo (rete permettendo)
  e confermare se il mic resta in ascolto in modo affidabile ora, e se
  la voce sembra piu' reattiva con lo streaming per frase.

## 16:57 — visione "JARVIS più potente di sempre": note vocali Telegram + 2 pannelli dashboard

- Fatto: chiesta una lista ampia di feature ("il JARVIS più potente di
  sempre"). Chiarito cosa esisteva già (YouTube via blocco browser, mail,
  spegnimento PC con conferma, dashboard/Telegram già utilizzabili da
  mobile senza altro lavoro) e cosa era da costruire davvero. Costruito:
  1. **Note vocali Telegram** (`bot.py::_transcribe_telegram_voice`):
     scarica il file OGG via getFile + download diretto, trascrive con
     lo stesso motore whisper del resto del repo, passa il testo a
     `handle()` esattamente come un messaggio scritto — nessun filtro
     parola d'attivazione (mandare una nota vocale e' gia' un'azione
     esplicita, diverso dal mic sempre acceso della dashboard).
  2. **Meteo settimanale animato** (`core/weather.py::get_weekly_forecast()`
     + `bot.py::weather_forecast_loop()`, pubblica su Turso ogni 30 min):
     nuovo pannello dashboard con un'icona animata per giorno (sole
     ruota, temporale trema, pioggia/neve/nuvole fluttuano) — richiesta
     esplicita "come il vero JARVIS di Iron Man". Si apre anche solo
     chiedendo "che tempo fa", non serve dire "apri".
  3. **Stato progetti visivo** (`core/project_status.py` esteso con un
     punteggio di "salute" 0-100 da segnali git REALI — albero pulito,
     recency ultimo commit, flag 🔴 nelle note — **onestamente etichettato
     come "salute", non "% di completamento"**: non esiste un task
     tracker per questi progetti, non ho inventato un numero senza base.
     `bot.py::project_status_loop()` pubblica su Turso ogni 10 min.
     Pannello con barra colorata + modifiche in sospeso + ultimo commit
     per Aura/TradeFlow/Whitesoul.
  Entrambi i pannelli: stesso pattern gia' rodato oggi per il flag
  "speaking" (tabella `runtime_flags` su Turso, bot.py scrive perche'
  gira solo in locale — git/Open-Meteo non raggiungibili dal browser).
  183/183 test verdi (20 nuovi). Mergiato e pushato su main. **Verificato
  dal vivo**: push reale su Turso per entrambi (meteo: Santa Eulària des
  Riu, 7 giorni; progetti: Aura 70%, TradeFlow 100%, Whitesoul 70% -
  coerente con l'attivita' reale gia' nota oggi); azione Vercel
  confermata deployata (risponde "Unauthorized" invece di "invalid
  action" ad una chiamata senza cookie di sessione — atteso, non un
  bug).
- Esito: ok, verificato lato server/dati. **Non verificato visivamente**
  (rendering pannelli, animazioni reali nel browser) — richiede
  Alessandro.
- Non costruito (spiegato, non frainteso come rifiuto): un vero
  streaming audio bidirezionale come la voce nativa di Claude
  richiederebbe una riscrittura architetturale, non incrementale.
- Prossimo passo: Alessandro deve riavviare il bridge (tutte le feature
  di oggi sono lato Python, serve il riavvio) e verificare a schermo i
  due nuovi pannelli (dock: ⛅ meteo, 🗂️ stato progetti) + provare una
  nota vocale su Telegram.

## 17:42 — Allucinazioni whisper su rumore/silenzio (root cause verificata, non ipotesi)
- Fatto: riletto logs/bot.log — flusso enorme di "trascrizioni" che sono in realta' allucinazioni whisper su rumore/silenzio ("Buon appetito!", "Sottotitoli e revisione a cura di QTSS", eco letterale dell'initial_prompt "Il maggiordomo AI di Iron Man."). Riprodotto isolatamente dando rumore gaussiano puro al modello: stesso identico output "Buon appetito!" con no_speech_prob=0.90. Applicato vad_filter=True (silero VAD di faster-whisper) in core/voice/stt.py — stesso rumore di test, trascrizione vuota; comando vero via TTS resta corretto. Aggiunta seconda rete in core/web_bridge.py: blocklist frasi note + wake word richiesta vicino all'inizio dell'enunciato (non piu' ovunque nella frase). 3 nuovi test in tests/test_web_bridge.py, suite completa verde (186 test). Committato e pushato (4a86267).
- Esito: ok (verificato con riproduzione isolata, non solo ipotesi — richiesta esplicita di Alessandro dopo la ricomparsa dell'errore Outlook)
- Imparato: l'initial_prompt su whisper (usato per il riconoscimento fonetico di "Jarvis") e' un'arma a doppio taglio — aiuta la trascrizione vera ma polarizza le allucinazioni su rumore verso l'eco del prompt stesso. vad_filter=True e' il fix corretto: scarta il rumore PRIMA della decodifica, non prova a filtrare il testo dopo.
- Prossimo passo: chiedere ad Alessandro di riavviare bot.py (autostart, non posso farlo io) e testare dal vivo qualche minuto — se il flusso di falsi trigger si ferma, verifica indiretta anche per Outlook (ipotesi: il carico del flood di trascrizioni spurie destabilizzava il processo). Non toccare Outlook direttamente finche' non si osserva se l'errore 0x80080005 si ripresenta con il mic ora silenzioso.

## 17:55 — Fix regressione: "Non ho capito niente" mostrato ad ogni rumore
- Fatto: subito dopo il fix vad_filter, Alessandro ha riportato che il messaggio "Non ho capito niente dall'audio, Signore. Riprova." continuava a comparire — causa trovata: vad_filter ora produce testo VUOTO su rumore (corretto), ma il vuoto era trattato come status="error" visibile invece che come "ignored" silenzioso (stesso trattamento gia' riservato alla mancanza di wake word). Cambiato in core/web_bridge.py. Aggiunto test di regressione (verificato manualmente che fallisce senza il fix). Committato e pushato (d246b81).
- Esito: ok (verificato: test fallisce su codice precedente, passa con il fix; suite completa 187 verde)
- Imparato: un fix lato server (vad_filter) puo' cambiare la *distribuzione* degli output (allucinazione -> vuoto) in un modo che rompe assunzioni a valle scritte per il vecchio comportamento — controllare sempre il percorso "vuoto"/"nessun risultato" dopo aver cambiato cosa il motore a monte restituisce in quel caso.
- Prossimo passo: stesso di prima — riavvio bot.py + test dal vivo per confermare che il flusso di falsi messaggi si sia fermato.
