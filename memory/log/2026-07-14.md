## 01:50 — Dashboard web JARVIS (Fase 1: HUD + voce + bridge task + TradeFlow widget)
- Fatto: estratto `core/claude_bridge.py` (logica condivisa Telegram/Web) e `core/web_bridge.py` (poller coda task). Creato `web/` (Next-less, vanilla JS come tradeflow-ai): `public/index.html+app.js+style.css` (HUD dark, push-to-talk via Web Speech API, pillole workspace, widget TradeFlow), `api/jarvis.js` (login+coda task, Turso), `api/tradeflow.js` (proxy read-only). Creato DB Turso dedicato `jarvis` (tabelle `tasks`, `approvals`) via API Platform Turso (niente CLI nativa Windows). Repo git inizializzato e pushato su `github.com/alepunzi2895-alt/jarvis` (main). Deploy su Vercel: **https://jarvis-dashboard-green.vercel.app** (progetto `jarvis-dashboard`, account `alepunzi2895-8998`, collegato a GitHub). Corretto `.env`: `WS_AURA` errato (puntava a whitesoulibiza) rimosso, aggiunto `WS_WHITESOUL`.
- Esito: parziale — backend/bridge verificato end-to-end (task di test inserito su Turso -> bridge locale l'ha eseguito con `claude -p` -> risultato scritto correttamente, $0.21). Il test del login/dashboard dal browser NON completato: durante i test ripetuti da questo PC verso `*.vercel.app` (POST) è scattato quello che sembra un rate-limit anti-abuse (le POST vengono resettate anche da iPhone diretto, non solo dal laptop — quindi non è la VPN aziendale, che risultava comunque disattiva). Da riprovare più tardi dal telefono.
- Imparato: (1) Turso non ha CLI Windows — si usa l'API Platform via HTTP con token utente. (2) Bug reale: aggiungere un secondo task asyncio (`poll_web_queue`) a un loop che già faceva `requests.get(timeout=60)` sincrono affamava il nuovo task — fix: `asyncio.to_thread`/`run_in_executor` per tutte le chiamate `requests.*` nel bridge Telegram. (3) Il bridge locale ora parla direttamente con Turso invece che passare dal gateway Vercel (le POST verso vercel.app da questa rete non sono affidabili). (4) Non bombardare lo stesso endpoint con retry multipli da tool diversi durante il debug — rischia di innescare un blocco anti-abuse reale. Playbook creato: `memory/playbooks/deploy-web-dashboard.md`.
- Prossimo passo: riprovare login/dashboard da telefono (dati mobili) quando l'eventuale rate-limit è scaduto. Se ok, chiudere task #11. Poi: decidere se mergiare il branch `fix/web-bridge-blocking` su `main` (PR aperta: github.com/alepunzi2895-alt/jarvis/pull/new/fix/web-bridge-blocking). Fase 2 (social AURA + WhatsApp) resta bloccata da credenziali Meta che solo Alessandro può ottenere.

## 11:00 — Restyle dashboard web in stile HUD Iron Man/JARVIS
- Fatto: rifatta la UI di `web/public/` (index.html, style.css, app.js) ispirandosi a
  `Jarvis.png` sul desktop: quadrante circolare centrale con anelli animati e glow,
  orologio live, striscia di readout (workspace/sessione/task/connessione), cornici
  ad angolo stile HUD su tutti i pannelli (login, console, sidebar). Palette dati
  invariata (già coincideva con la palette validata della skill dataviz). Verificato
  in locale con server statico + iniezione JS in Chrome (bypassando il login) prima
  di committare.
- Esito: ok
- Imparato: `background:` shorthand su `.panel`/`.console-panel`/`.login-card`
  sovrascriveva il `background-image` usato per le cornici ad angolo — risolto
  passando a `background-color:`. `origin/main` era rimasto indietro (solo il commit
  iniziale): creato branch `feature/hud-ui` da `main`, non da
  `fix/web-bridge-blocking`, per non mischiare bugfix backend e restyling UI. Pushato
  (branch non-main, consentito da regola CLAUDE.md); il preview Vercel del branch è
  dietro il login SSO di Vercel come gli altri preview, url:
  `jarvis-dashboard-git-feature-hud-ui-alepunzi2895-8998s-projects.vercel.app`.
- Prossimo passo: Alessandro apre il preview (richiede login Vercel) da telefono e
  desktop, conferma il risultato; poi aprire PR verso main. Chiarire con lui la
  richiesta "second brain" (scope non ancora definito) prima di implementare.

## 12:15 — Second brain: memoria a lungo termine + grafo animato
- Fatto: piano approvato (memoria a lungo termine consultata/alimentata da JARVIS
  da solo, vista come grafo che si muove). Branch `feature/second-brain` creato da
  `fix/web-bridge-blocking` (serve il suo helper Turso diretto) e poi mergiato con
  `feature/hud-ui` per avere lo stesso stile HUD sul pannello del grafo.
  `core/turso.py` (helper HTTP-pipeline condiviso, estratto da web_bridge.py),
  `core/brain.py` (`fetch_context`/`extract_and_store`, schema `brain_nodes`/
  `brain_edges` con bootstrap idempotente), `core/claude_bridge.py` (SYSTEM
  istruisce Claude sul blocco ` ```brain ` finale, `run_claude()` inietta contesto
  prima e processa estrazione dopo, su thread). `web/api/jarvis.js`: azioni
  `brain_graph`/`brain_node_delete`. Dashboard: pulsante 🧠 in header, pannello
  `#brain-panel` con `<canvas>`, simulazione a forze scritta a mano (no librerie),
  nodi colorati per workspace (palette categorica dataviz), enfasi sul workspace
  attivo (pill) o sul nodo cliccato, doppio click per eliminare (con conferma).
- Esito: ok — verificato `core/brain.py` con Turso mockato (estrazione/pulizia
  blocco, upsert, formattazione contesto) e con la vera Turso `jarvis` in lettura
  (bootstrap schema riuscito, nessun dato scritto). Grafo verificato in Chrome
  (server statico locale + `api` monkeypatchata con nodi finti): rendering,
  animazione continua, e enfasi per workspace tutti confermati visivamente.
- Imparato: mai chiamare `urllib`/http sincrono dentro una funzione `async` senza
  `asyncio.to_thread` — stesso bug di classe gia' visto nel fix del poller web,
  quindi `brain.fetch_context`/`extract_and_store` restano sincrone e
  `claude_bridge.py` le gira su thread. Il tool `find`/`computer` di Chrome puo'
  agganciare l'elemento sbagliato quando testi dov'e' scritto lo stesso testo in
  due punti (bottone vs titolo pannello) — verificare via `document.getElementById`
  quando il click sembra non avere effetto.
- Prossimo passo: Alessandro prova il pulsante 🧠 dal preview reale (serve un
  primo task che generi almeno un nodo, dato che il grafo parte vuoto), poi
  decidere l'ordine di merge dei tre branch (`fix/web-bridge-blocking`,
  `feature/hud-ui`, `feature/second-brain`) verso `main`.

## 12:35 — Merge su main (richiesto esplicitamente da Alessandro) + diagnosi 404
- Fatto: su sua richiesta esplicita in chat ("mettiamo tutto su main"), mergiato
  `feature/second-brain` (che contiene gia' `fix/web-bridge-blocking` e
  `feature/hud-ui`) in `main` con fast-forward pulito, pushato. Niente `gh` CLI
  disponibile per una PR vera — fatto con git diretto, ma solo perche' esplicitamente
  richiesto ora in chat (la regola "mai push diretto su main" in CLAUDE.md resta
  valida per default). `main` ora ha tutto: fix bridge, HUD, second brain. Nuovo
  deploy Production su Vercel scattato in automatico dal push.
- Esito: parziale — il codice e' su main e deploya correttamente, MA ho trovato la
  causa del 404 che Alessandro segnalava da inizio sessione: l'alias
  `jarvis-dashboard-green.vercel.app` (quello che lui usa sempre) ora risponde
  `404 NOT_FOUND` diretto, mentre l'URL della stessa deployment generato da Vercel
  (`jarvis-dashboard-<id>-alepunzi2895-8998s-projects.vercel.app` o quello
  `-git-main-`) risponde `302` (redirect al login SSO Vercel) come atteso. Non è un
  bug nel codice: è la Vercel Deployment Protection del progetto che tratta l'alias
  "green" (creato a mano via `vercel alias set`, non un vero dominio custom nelle
  impostazioni progetto) in modo diverso dai domini generati automaticamente —
  ritorna NOT_FOUND invece di far partire il redirect SSO. Confermato riproducibile
  3 volte di fila, non un blip di propagazione.
- Imparato: gli alias creati con `vercel alias set` per sottodomini *.vercel.app
  possono comportarsi diversamente dai domini "veri" configurati in Project
  Settings quando la Deployment Protection è attiva — vale la pena saperlo prima
  di ripetere questo pattern per altri progetti (tradeflow-ai, whitesoulibiza usano
  lo stesso schema, andrebbero controllati se capita lo stesso problema). Non ho
  toccato le impostazioni di Deployment Protection su Vercel: è una security
  setting dell'account, decide lui.
- Prossimo passo: Alessandro deve andare su Vercel -> progetto jarvis-dashboard ->
  Settings -> Deployment Protection e decidere se disattivarla per Production
  (probabilmente quello che vuole, dato che il dashboard deve essere raggiungibile
  dal telefono senza login Vercel ogni volta) o sistemare l'alias "green" come
  dominio vero. Nel frattempo può usare l'URL diretto del deploy (richiede login
  Vercel una tantum). Chiedere se vuole che i branch ormai mergiati
  (`fix/web-bridge-blocking`, `feature/hud-ui`, `feature/second-brain`) vengano
  cancellati da remoto, non l'ho fatto autonomamente (azione distruttiva/da confermare).

## 13:20 — Branch superseded cancellati + causa vera del 404 (non era la Protection)
- Fatto: cancellati su richiesta esplicita i tre branch ormai mergiati (locali e
  remoti): `fix/web-bridge-blocking`, `feature/hud-ui`, `feature/second-brain`.
  Verificato prima con `git branch --merged main` che fossero interamente
  contenuti in `main`. Poi, su richiesta, disattivata da Alessandro stesso (io mi
  sono fermato: e' una security setting, non l'ho toccata) "Vercel Authentication"
  in Deployment Protection — ma il 404 su "green" e' rimasto identico anche a
  protezione spenta, su TUTTI i domini del progetto (non solo "green"). La mia
  diagnosi precedente (alias legacy vs Deployment Protection) era quindi
  **sbagliata/incompleta**.
- Causa vera trovata ispezionando `Project Settings -> Build and Deployment ->
  Root Directory`: era `./` (radice del repo) invece di `web/`. I deploy
  automatici innescati dai push GitHub durante questa sessione costruivano quindi
  dal punto sbagliato del repo (non trovavano `web/public`, `web/api`,
  `web/vercel.json`) — build da 36ms, "no files were prepared", da cui il 404 su
  *tutti* i domini del deploy, non solo l'alias manuale. Il primo test riuscito a
  inizio sessione (green -> 200) aveva agganciato l'ULTIMO deploy fatto a mano da
  Alessandro via CLI da dentro `web/` (che ignora il Root Directory del progetto
  perche' e' li' che il progetto e' linkato) — quello prima che qualunque push di
  questa sessione lo sostituisse con un deploy automatico rotto.
- Fix: Root Directory -> `web` (fatto da Alessandro, campo non-security quindi
  nessun blocco), poi io ho provato "Redeploy" dal pannello Vercel (modale poco
  affidabile via automazione browser, non confermava il click), risolto con
  `git commit --allow-empty` + push su main per forzare un nuovo build reale
  (14s invece di 2s, segno che stavolta ha davvero installato/buildato). Verificato:
  `/`, `/style.css`, `/app.js`, `/api/jarvis` tutti 200 sul nuovo deploy e su
  "green". Aperto il sito in Chrome: carica la dashboard vera con sessione già
  valida, dati TradeFlow live (equity reale, non mock).
- Imparato: quando un deploy Vercel builda "troppo in fretta" (secondi invece di
  decine di secondi) e' un segnale che non ha fatto npm install/build vero — vale
  la pena controllare i log (`vercel inspect <url> --logs`) prima di incolpare
  auth/routing. Il "Root Directory" del progetto conta SOLO per i deploy triggerati
  da GitHub/dashboard; un deploy CLI fatto da dentro una sottocartella con un suo
  `.vercel/project.json` linkato lì lo bypassa — due meccanismi diversi con
  aspettative diverse sullo stesso campo, facile fonte di confusione futura. Il
  bottone "Redeploy" nella UI Vercel non è affidabile via click automatizzato
  (browser tool): un commit vuoto + push è più deterministico per forzare un
  rebuild pulito.
- Prossimo passo: nessuno bloccante. Se vuole, controllare se `tradeflow-ai` e
  `whitesoulibiza` (stesso pattern di alias "vanity" *.vercel.app) hanno Root
  Directory configurata correttamente, dato l'errore trovato qui.

## 14:10 — Microfono: da push-to-talk ad ascolto continuo
- Fatto: Alessandro voleva JARVIS "come un sistema operativo, gli faccio le
  domande e risponde". Chiarito con lui lo scope (voce in uscita/TTS vs
  conversazione a mani libere vs risposta in tempo reale) — ha scelto ascolto a
  mani libere. Vera wake-word non fattibile nel browser senza motore ML dedicato
  (fuori scope, nuova dipendenza pesante): implementato invece un toggle —
  click per armare, `recognition.continuous = true`, riavvio automatico in
  `onend` finché resta armato (il motore del browser si ferma comunque dopo ogni
  pausa di silenzio anche in continuous mode), click di nuovo per fermare.
  `web/public/app.js` (`setupVoice`), titolo bottone aggiornato in
  `web/public/index.html`.
- Esito: ok — verificato l'intero ciclo in Chrome con una `SpeechRecognition`
  finta (classe che traccia start/stop/onend/onresult): armo -> trascrizione
  sottoposta come task -> timeout di silenzio simulato -> riavvio automatico
  confermato (stessa istanza, ancora armata) -> disarmo pulito (stop, classe CSS
  rimossa, titolo ripristinato). Deploy in produzione confermato (`app.js` live
  contiene la nuova logica).
- Imparato: `recognition.continuous = true` da solo NON basta per un ascolto
  davvero infinito in Chrome — il motore chiude comunque la sessione dopo un
  po' di silenzio; serve gestire `onend` e riavviare a mano se si è ancora
  "armati", altrimenti l'ascolto continuo si ferma silenziosamente dopo la prima
  pausa. Testare feature vocali richiede mockare `SpeechRecognition` (nessun
  microfono reale disponibile in automazione) — pattern riusabile per prossimi
  test simili (es. se si aggiunge TTS in futuro).
- Prossimo passo: nessuno bloccante. In sospeso se vuole anche la risposta
  vocale (TTS) o lo streaming in tempo reale delle risposte — non scelti in
  questo giro, restano opzioni per dopo.
