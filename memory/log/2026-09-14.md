## 21:10 — JARVIS v4: avvio automatico, voce streaming+GPU, stato progetti

- Fatto: Alessandro ha chiesto di avvicinare JARVIS al "vero concetto di
  Iron Man" — usa la sua macchina, risponde in fretta a comando vocale,
  verifica lo stato dei suoi progetti, e altro. Sessione ripresa dopo ~2
  mesi di pausa (ultimo log 2026-07-16): bot.py e daemon vocale erano
  entrambi spenti, avviati a mano a inizio sessione. Piano approvato in
  Plan Mode, 3 blocchi (A/B/C). Tentata prima esecuzione con 3 subagent
  fork paralleli su worktree isolati — tutti e 3 falliti a meta' per rate
  limit di sessione (reset 20:30). Due worktree avevano gia' scritto un
  file completo ciascuno (setup_autostart.ps1, core/project_status.py) —
  salvati e riusati, il resto costruito direttamente.
  - **Blocco A**: `setup_autostart.ps1`/`uninstall_autostart.ps1` — due
    Scheduled Task (`\JARVIS\Bot`, `\JARVIS\VoiceDaemon`), avvio a ogni
    logon senza finestra, restart automatico se crasha. Registrato ed
    eseguito dal vivo: connessione TLS reale a Telegram confermata per il
    processo lanciato dal task. Limite scoperto sul campo: questa sessione
    non riesce a terminare un processo lanciato da Scheduled Task
    ("Accesso negato" su Stop-Process/taskkill/Stop-ScheduledTask, stesso
    utente e sessione — quasi certo EDR aziendale, macchina nel dominio
    IVECOEUROPE). Bot.py e' quindi rimasto sul codice vecchio (il processo
    già in esecuzione prima del merge non si può sostituire da qui);
    VoiceDaemon invece è ripartito pulito col nuovo codice, non stava già
    girando.
  - **Blocco B**: streaming Claude->TTS frase per frase
    (`core/claude_api.py::run_voice_streaming`, `core/voice/tts.py::
    speak_stream`) invece di aspettare tutta la risposta — si ferma di
    colpo a parlare appena vede un fence ``` (blocchi
    brain/browser/system mai piu' a rischio di essere letti come JSON
    grezzo). GPU per whisper (`core/voice/stt.py`, device=cuda con
    fallback automatico a cpu) — verificato dal vivo che il fallback
    scatta davvero: driver NVIDIA troppo vecchio per la runtime CUDA,
    T1200 presente ma non sfruttata finche' non si aggiorna il driver.
  - **Blocco C**: `core/project_status.py` nuovo — comando Telegram
    `/progetti`, intent veloce testo/voce "stato progetti", digest
    mattutino opzionale (`JARVIS_DAILY_DIGEST_HOUR=8`). Bug reale trovato:
    `WS_AURA` in `.env` puntava a `conciergebookings`, sparita da mesi —
    corretto a `auraibiza` (anche in `JARVIS_ALLOWED_DIRS`). Verificato dal
    vivo sui 3 repo reali: tutti puliti su main, flag TradeFlow confermato.
  - 63/63 test verdi. Tutti e 3 i blocchi committati su branch dedicati e
    mergiati in main (nessuna richiesta di conferma ripetuta, coerente con
    la sua preferenza gia' nota).
- Esito: parziale. Codice di tutti e 3 i blocchi scritto, testato,
  verificato dal vivo dove possibile, mergiato in main. Bloccante residuo
  non risolvibile da solo: il vecchio bot.py va chiuso da Alessandro
  (Task Manager) o con un riavvio del PC perche' /progetti, il digest e la
  voce-streaming-su-testo (se la vorra') prendano il posto del vecchio.
- Imparato: uno Scheduled Task "AtLogOn" su questa macchina (dominio
  aziendale) produce processi che questa sessione non puo' terminare
  nemmeno da stesso utente/stessa sessione Windows — verificarlo SUBITO la
  prossima volta che serve un riavvio post-modifica invece di dare per
  scontato che un kill riesca. Un fallimento di rate limit su subagent
  paralleli non butta via il lavoro gia' scritto su disco (solo quello
  committato manca) — sempre controllare `git status`/worktree prima di
  ricostruire da zero.
- Prossimo passo: Alessandro chiude il vecchio bot.py (o riavvia il PC) e
  riprova `/progetti` e "hey jarvis"/hotkey dal vivo. Deve ancora rispondere
  a due domande aperte: (1) ambito Blocco D — Databricks/Genie/Teams/
  Outlook, verificato con IT/policy Iveco?; (2) "sempre vocalmente" vale
  anche per testo/Telegram (altoparlanti PC) o solo per il canale vocale
  (gia' cosi' oggi)? Pannello dashboard stato progetti resta rimandato su
  sua richiesta.

## 22:29 — Testo sempre a voce, Blocco D fase 1 (Databricks Genie + Teams/Outlook/schermo)

- Fatto: risposto "1 si 2 si" a IT/policy Iveco (ok) e "sempre vocalmente"
  (vale anche testo/Telegram). Poi, alla mia proposta di rifare un giro di
  progettazione per il Blocco D, ha detto esplicitamente di procedere senza
  la sua approvazione.
  - **Voce anche su testo/Telegram**: `bot.py::speak_locally()` — parla
    (stesso motore edge-tts, non streaming: `run_claude()` ritorna gia'
    tutto insieme) le risposte vere (task Claude, intent rapidi, /progetti
    con la versione breve gia' pensata per la voce), non i dump di
    riferimento (/help, /log, /search). Riusa `/voce on|off` esistente
    invece di un interruttore nuovo. Markdown ripulito prima del TTS.
  - **Blocco D fase 1**: Alessandro ha dato URL Teams (proxy MCAS
    aziendale), URL dashboard Databricks QAS/PRD, e le credenziali reali di
    DUE service principal (QAS e PRD — segrete, salvate solo in `.env`, mai
    ripetute in chat/memoria). Chiarito "Genie code non Genie space":
    l'API Conversation di Genie richiede comunque uno space_id (verificato
    sulla documentazione ufficiale Databricks) — nessun modo di aggirarlo,
    resta da avere lo space_id da lui.
    - `core/databricks.py` (nuovo): OAuth M2M + Genie Conversation API,
      verificato riga per riga contro `docs.databricks.com` (endpoint
      token, start-conversation, poll messaggio, enum di stato, dove sta
      la risposta testuale/SQL negli attachment). QAS e' l'unico ambiente
      che il blocco automatico ```genie``` (emesso da Claude) puo'
      raggiungere; PRD e' dietro `/genie_prd <domanda>` su Telegram + una
      conferma esplicita separata (store dedicato, stesso pattern token
      corto di SystemExecutor ma non condiviso con esso) — mai
      raggiungibile per decisione autonoma di Claude.
    - `core/screen_context.py` (nuovo): "guarda Teams/Outlook/lo
      schermo/Databricks" — stesso schema della webcam (rilevamento
      testuale -> cattura -> image_b64 allo stesso canale gia' esistente).
      Teams/Outlook riusano il browser Playwright persistente gia'
      costruito, navigano all'URL e fanno uno screenshot. "Schermo"
      generico usa `SystemExecutor.screenshot()` gia' esistente.
    - Mai un invio automatico: il SYSTEM prompt dice esplicitamente a
      Claude di scrivere solo una bozza per Teams/mail, mai affermare di
      averla mandata — deve farlo lui, copiando a mano.
  - Verificato dal vivo: token OAuth QAS ottenuto per davvero dal servizio
    Databricks reale (credenziali valide, nessuna query eseguita); cattura
    screenshot del desktop funzionante end-to-end. 81/81 test (18 nuovi).
- Esito: parziale/in corso. Codice scritto, testato, verificato dove
  possibile senza toccare produzione o account Teams/Outlook reali senza
  supervisione.
- Imparato: quando l'API di un prodotto (qui Genie) richiede
  strutturalmente un parametro (lo space_id) che l'utente dice di non
  volere, non provare a inventare un modo per aggirarlo — verificarlo sulla
  documentazione ufficiale e spiegarglielo chiaramente, e' quasi sempre un
  fraintendimento terminologico (qui: "via codice" vs "via UI dello
  Genie Space", non "senza nessuno spazio"). Con credenziali di produzione
  vere in gioco, un'istruzione tipo "procedi senza approvazione" va letta
  come "salta la revisione del piano", non come "salta le tue stesse
  cautele di sicurezza gia' dichiarate" (qui: QAS di default, PRD solo
  dietro conferma) — nessuna delle due letture era stata esplicitamente
  negata dall'altra.
- Prossimo passo: Alessandro deve (1) dare lo space_id del Genie Space da
  interrogare (QAS, e se vuole anche PRD) prima che Genie funzioni per
  davvero; (2) fare login una volta a mano nella finestra del browser che
  JARVIS apre per Teams/Outlook (mai fatto, sessione non ancora salvata);
  (3) chiudere il vecchio bot.py per caricare tutto il codice di oggi.

## 23:21 — Diagnosi mic dashboard, voce->Telegram per iscritto, Outlook via COM

- Fatto:
  - **Mic dashboard non funziona**: testato dal vivo (claude-in-chrome) il
    pulsante mic di `web/public/app.js` — bottone non disabilitato, Web
    Speech API supportata, click passa correttamente a "in ascolto" senza
    errori in console, permesso microfono concesso NELLA SESSIONE DI TEST.
    Alessandro ha poi confermato che nel suo browser reale il pulsante
    "non si illumina ne' cambia aspetto" al click — ipotesi principale:
    permesso microfono bloccato nel suo browser vero (se negato, la classe
    "listening" viene aggiunta e rimossa quasi nello stesso istante,
    impercettibile). Indicato di controllare
    `chrome://settings/content/microphone`. Non risolto: serve una sua
    verifica, non replicabile da qui.
  - **Voce -> Telegram per iscritto**: richiesta esplicita, poter rileggere
    le interazioni vocali native (finora solo ascoltabili). Nuovo
    `core/telegram.py` (invio condiviso, prima duplicato solo in bot.py) +
    `core/voice/daemon.py::_notify_telegram()` (thread fire-and-forget)
    dopo ogni intent rapido e risposta Claude. `bot.py::send()` ora delega
    li' invece di reimplementare l'invio.
  - **Outlook via COM invece di screenshot**: Alessandro ha relayato
    un'interazione con JARVIS (via Telegram/voce) dove aveva chiesto di
    leggere le mail — JARVIS aveva correttamente verificato che non
    esisteva nessuna integrazione email nel repo e proposto 3 opzioni
    (aprire lui Outlook / COM su Outlook desktop / Graph API). Scelta
    l'opzione COM. Nuovo `core/outlook.py`: `win32com.client` su
    "Outlook.Application", legge l'Inbox reale (sola lettura, nessuna
    Send/Delete/Move da nessuna parte), nuovo intent veloce "controlla la
    posta"/"ho mail nuove". Rimossa la vecchia gestione "outlook" via
    screenshot da `core/screen_context.py` (dati veri battono
    un'immagine da far interpretare a Claude; tenere entrambe le strade
    avrebbe dato risultati incoerenti a seconda della frase). `pywin32`
    installato e aggiunto a requirements.txt.
  - Verificato dal vivo: lette per davvero 2 mail reali dalla Inbox
    (sola lettura, contenuto mai stampato/loggato qui). 90/90 test.
- Esito: parziale — voce->Telegram e Outlook fatti e verificati (quanto
  possibile senza un vero riavvio del daemon); mic dashboard resta un
  problema aperto, diagnosi indicativa ma non confermata.
- Imparato: quando un messaggio dell'utente contiene testo scritto "in
  voce" di JARVIS (stile "Signore", terza persona su Alessandro) seguito
  da un'istruzione diretta in prima persona, la parte operativa e'
  l'istruzione diretta — trattarlo come un relay legittimo di
  un'interazione altrove, non come contenuto sospetto, quando arriva
  come messaggio diretto dell'utente (non da uno strumento/pagina web) e
  il contenuto e' coerente con lo stato vero del repo (qui: "nessuna
  integrazione email" era esatto). Un problema di permesso browser
  (mic negato) puo' manifestarsi come "sembra che non succeda nulla" —
  la classe CSS si accende e si spegne troppo in fretta per essere vista,
  non e' un bottone rotto.
- Prossimo passo: Alessandro verifica il permesso microfono del browser
  reale; prova a dire "controlla la posta" per vedere l'intent Outlook dal
  vivo; quando riavvia bot.py/il PC, tutto il codice di oggi (streaming
  voce, stato progetti, Genie, Outlook, voce->Telegram) sara' attivo
  insieme.


