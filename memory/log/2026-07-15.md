## 00:30 — JARVIS "vero": HUD a finestre fluttuanti, camera, second brain↔Obsidian
- Fatto: Alessandro ha condiviso `Jarvis.jpeg` come riferimento (palla centrale
  con scritta JARVIS, pannelli fluttuanti tipo "CAM_FEED", griglia scura,
  puntini decorativi) e chiesto: vedere JARVIS come vero sistema operativo,
  camera che si accende in una finestra, finestre che fluttuano intorno alla
  palla per ogni azione (camera, chat), controllo di tutto il sistema
  operativo. Pianificato in Plan Mode (nuovo piano, quello precedente su
  EnvironmentRouter/MCPRouter resta in pausa): spiegato il vincolo non
  negoziabile che la dashboard web non potrà mai lanciare app native
  (sandbox del browser) — solo il bridge locale può farlo.
  Implementato blocco (c1) su branch `feature/jarvis-c1-floating-hud`:
  - `web/public/*`: HUD riscritto — palla centrale fissa e animata (respiro +
    deriva), pannelli esistenti (console, TradeFlow, second brain, social)
    diventati finestre fluttuanti apribili da un dock in basso o via drag
    dalla barra del titolo; sfondo a griglia con punti decorativi.
  - Pannello camera: `getUserMedia`, cattura frame + domanda, stream sempre
    fermato alla chiusura.
  - Comandi vocali "Jarvis apri/accendi/chiudi X" intercettati lato client
    prima di sottoporre un task — istantanei, nessuna chiamata a Claude.
  - Round-trip immagine → Claude: nuova colonna `tasks.image_b64` (Turso +
    `web/api/jarvis.js`), `core/web_bridge.py` la passa a
    `core/claude_bridge.py` che la scrive in un file temp (`.tmp/`,
    gitignored) e la referenzia nel prompt — Claude la legge col proprio
    Read tool, zero dipendenze di visione nuove.
  - `core/brain.py`: ogni nodo/edge del second brain ora si specchia come
    nota reale in Obsidian (`SecondBrain/<slug>.md`), senza mai sovrascrivere
    il corpo di una nota già esistente (solo frontmatter aggiornato) — così
    eventuali modifiche manuali di Alessandro nel vault non si perdono.
  - `core/system_executor.py`: `APP_REGISTRY` esteso + risoluzione dinamica
    via registro Windows "App Paths" (copre Chrome/VS Code automaticamente,
    senza doverli elencare a mano) + fallback per app Electron non
    registrate (Obsidian, path noto). Nuovo comando `/apri <app>` su Telegram.
- Esito: ok — 11/11 test passano. Verificato tutto end-to-end con dati reali,
  non solo mock: immagine vera inviata a Claude (ha descritto correttamente
  un collage di paesaggi), task reale che genera un nodo second-brain
  verificato sia su Turso sia come nota vera nel vault
  (`SecondBrain/jarvis-dashboard-accent-color.md`), Notepad aperto per
  davvero via `SystemExecutor.open_app`, drag/apertura/chiusura finestre
  testati in Chrome con camera/API mockate.
- Imparato: `hidden` attribute HTML può essere silenziosamente sovrascritto
  da una regola CSS che imposta `display` sulla stessa classe (l'attributo
  `[hidden]` dell'user-agent perde contro qualunque regola author-stylesheet,
  a prescindere dalla specificità) — serve sempre un `.classe[hidden] {
  display: none }` esplicito quando si combinano le due cose. App Electron
  "portable-style" (Obsidian) spesso non si registrano tra le App Paths di
  Windows anche se Chrome/VS Code lo fanno — serve un fallback a path noti.
- Prossimo passo: mergiato in main su richiesta esplicita (dopo aver
  verificato che il preview non poteva funzionare per le env var mancanti su
  Preview). Poi ritoccata la palla centrale su richiesta: più grande,
  animazione hue-rotate continua, satelliti orbitanti, drift più organico —
  anche questo committato direttamente su main (modifica visiva piccola su
  feature già approvata).

## 02:00 — JARVIS v2 blocco (c2): voce nativa (wake word, STT, TTS, persona)
- Fatto: Alessandro ha chiesto di essere guidato sulle decisioni per la voce.
  Proposto e concordato: openWakeWord con modello preaddestrato "hey_jarvis"
  (esiste davvero tra i modelli ufficiali del progetto, verificato — nessun
  training necessario), faster-whisper "small" su CPU (il suo i7-11850H a 8
  core lo regge bene), edge-tts gratis per iniziare (no API key; ElevenLabs
  restava un'opzione a pagamento per dopo, non scelta ora). Pianificato in
  Plan Mode, poi implementato su branch `feature/jarvis-c2-voice`:
  - `core/voice/wake_word.py`, `stt.py`, `tts.py`, `daemon.py`: loop
    completo wake word -> registra fino al silenzio -> trascrive -> spinge
    un task sulla STESSA coda Turso di `core/web_bridge.py` (non un motore
    d'esecuzione parallelo) -> aspetta il risultato -> risponde a voce.
    Ripetere la wake word mentre parla interrompe il TTS (invece di un
    secondo modello per "stop", semplificazione dichiarata).
  - `core/persona.py` + `core/claude_bridge.py`: nuovo parametro
    `channel="voice"` su `run_claude()`, applica la persona ("Signore",
    British, gentile, risposte max due frasi) SOLO al canale vocale.
  - Richiesta aggiuntiva di Alessandro nel mezzo: JARVIS deve sapere che si
    chiama Alessandro e rivolgersi sempre con gentilezza — aggiunta una
    regola di cortesia generale anche al `SYSTEM` di base (tutti i canali,
    non solo voce), oltre al tono "Signore" specifico della persona vocale.
  - `bot.py`: comando `/voce on|off` per silenziare il daemon (letto/scritto
    su `state.json`, il daemon lo rilegge da disco ad ogni ciclo per vedere
    i cambi fatti dall'altro processo).
  - `start_voice.bat` (stesso pattern di `start.bat`), dipendenze aggiunte a
    `requirements.txt`.
- Esito: parziale, dichiarato onestamente ad Alessandro. Verificato con audio
  vero (non solo mock): il modello wake-word carica ed è a ~0 su silenzio
  ambientale; faster-whisper trascrive perfettamente audio sintetizzato;
  edge-tts sintetizza e riproduce per davvero dagli altoparlanti; un task
  reale con `channel='voice'` genera una risposta breve in stile persona
  ("Signore", tono cordiale); flag `/voce on|off` persiste e si rilegge da
  disco correttamente. Riverificato che i comandi già esistenti (apri
  Chrome via `SystemExecutor.open_app`, apri chat/webcam nel browser) non
  sono stati toccati/rotti dai cambi di oggi.
  NON verificato (richiede lui): il rilevamento della wake word con la SUA
  voce reale nel microfono fisico. Un test sintetico
  altoparlante->microfono ha dato punteggio quasi nullo (1e-5); bypassando
  il microfono fisico via "Stereo Mix" (cattura diretta dell'audio in
  uscita, senza percorso acustico) il punteggio è salito a 0.11 — ancora
  sotto soglia (0.5) ma un ordine di grandezza più alto, a conferma che la
  cancellazione d'eco del microfono del laptop (pensata apposta per non
  captare l'audio dei propri altoparlanti) è la causa più probabile del
  quasi-zero, non un problema del modello. Serve un test dal vivo.
- Non fatto (fuori scope oggi, andrebbe pianificato a parte): "vai su
  YouTube, cerca X, metti il primo risultato" — richiede automazione
  browser reale (Playwright), è il blocco (d) del piano originale mai
  iniziato. Comunicato chiaramente ad Alessandro che questo non funziona
  ancora, per non fargli credere il contrario.
- Imparato: un test sintetico "altoparlante che riproduce la wake word,
  microfono che ascolta" non è un proxy affidabile su un laptop — l'AEC
  (Acoustic Echo Cancellation) del microfono è progettata apposta per
  sopprimere l'audio dei propri altoparlanti, quindi un punteggio basso in
  quel test NON significa che il modello non riconoscerebbe una voce umana
  reale. Il dispositivo "Stereo Mix" (cattura diretta dell'uscita audio,
  bypassando fisicamente il microfono) è un modo utile per isolare "il
  modello riconosce il contenuto audio" da "il microfono fisico lo capta" —
  vale la pena ricordarlo per futuri test simili.
- Prossimo passo: Alessandro deve avviare `start_voice.bat` e provare a dire
  "hey jarvis" dal vivo per il verdetto reale. Branch pushato, NON mergiato
  (in attesa del suo test) — a differenza del blocco (c1) però qui il rischio
  di mergiare comunque sarebbe basso (è tutto backend, un processo opzionale
  che non parte da solo), quindi mergiabile su sua richiesta anche prima del
  test vocale dal vivo, se preferisce.

## 13:00 — Fix microfono reale + blocco (d): automazione browser (Playwright)
- Fatto: Alessandro ha provato il daemon vocale e ha riportato "non sente il
  microfono". Diagnosticato subito (accesso diretto alla sua macchina):
  Windows aveva cambiato il device di **input predefinito** su una cuffia
  Jabra (probabilmente collegata per chiamate di lavoro) — il daemon ascoltava
  lì, non sul microfono del portatile. Aggiunto `JARVIS_MIC_DEVICE` (nuovo
  `core/voice/__init__.py::resolve_input_device()`, sottostringa case-insensitive
  del nome device, usata sia in `wake_word.py` sia in `stt.py`) per pinnare il
  microfono per nome invece di affidarsi al default di Windows, che può
  cambiare da solo. Impostato su "Intel" nel suo `.env`. Daemon riavviato.
  Contestualmente ha chiesto di implementare l'automazione Chrome (blocco d,
  mai iniziato). Pianificato in Plan Mode: `core/browser.py` (`BrowserAgent`,
  Playwright, contesto Chromium persistente in `.browser_profile/`) con lo
  stesso pattern del second brain — Claude emette un blocco ` ```browser``` `
  quando capisce che serve navigare un sito vero, `core/claude_bridge.py` lo
  estrae ed esegue. Sicurezza tramite vocabolario limitato (solo
  navigazione/ricerca/screenshot, niente click/fill generico) invece che un
  filtro sulle azioni pericolose — stessa filosofia di `SystemExecutor`.
- Esito: ok, verificato con YouTube vera (non un mock): "vai su youtube, cerca
  franco battiato/centro di gravità permanente, apri il primo risultato" →
  Chrome si apre davvero, cerca, clicca il primo risultato corretto, il video
  giusto parte (controllato anche con uno screenshot del browser Playwright).
  Verificato sia col modulo diretto sia passando da un task Claude reale.
  Confermato che una domanda normale (quanto fa 12×8) non apre il browser per
  errore. 11/11 test passano.
- Durante i test: **due blip di rete transitori** sul suo PC (DNS irrisolvibile
  per `cdn.playwright.dev` durante il download di Chromium, poi un timeout di
  connessione verso Turso durante un test) — entrambi risolti da soli in
  pochi minuti, non bug nel codice. Ormai un pattern ricorrente per questa
  macchina, vedi [[network-quirks-this-pc]] nella memoria globale.
  **Bug mio**: nel mezzo dei test ho ottenuto un blocco ` ```browser``` ` non
  ripulito nella risposta — causa: avevo riavviato il bridge PRIMA di scrivere
  `core/browser.py`, quindi il processo in esecuzione usava ancora il vecchio
  `core/claude_bridge.py` senza il wiring. Riavviato di nuovo e il test è
  passato pulito. Promemoria per me: dopo ogni modifica a `core/*.py` che il
  bridge importa, va riavviato — non basta salvare il file.
- Fatto anche: Alessandro ha detto esplicitamente "mergia tutto su main
  sempre" — mergiati blocco (c2) e blocco (d) in main, push fatto. Branch
  `feature/jarvis-c2-voice` e `feature/jarvis-d-browser` non cancellati (non
  richiesto esplicitamente).
- Prossimo passo: nessuno bloccante. In sospeso: conferma finale di
  Alessandro che "hey jarvis" ora lo sente col microfono giusto; blocco (b)
  (EnvironmentRouter/MCPRouter) resta in pausa, manca ancora il path Iveco.

## 13:56 — Riavvio daemon vocale fallito, poi risolto (interprete sbagliato)
- Fatto: un task in background di riavvio del daemon vocale (con il fix
  microfono) era fallito con exit code 1. Diagnosticato lanciandolo io stesso
  in foreground: usava il `python` globale
  (`AppData\Local\Programs\Python\Python312\python.exe`) invece
  dell'interprete del venv del progetto, e quello globale non ha
  `sounddevice` installato (`ModuleNotFoundError`). `start_voice.bat` è
  corretto (usa già `.venv\Scripts\python`) — il riavvio fallito doveva
  essere stato lanciato a mano con `python` nudo invece che tramite il
  `.bat` o l'interprete del venv.
- Esito: ok — riavviato con `.venv/Scripts/python.exe -u -m
  core.voice.daemon`, log conferma `daemon vocale attivo — di' "hey
  jarvis"`, nessun crash.
- Imparato: su questa macchina esistono DUE interpreti Python (globale in
  `AppData\Local\Programs\Python\Python312` + venv del progetto in
  `.venv\`) — solo il venv ha le dipendenze di JARVIS. Qualunque comando
  lanciato a mano (non tramite `start.bat`/`start_voice.bat`) deve usare
  esplicitamente `.venv/Scripts/python.exe`, mai `python` nudo.
- Prossimo passo: stesso di prima — in attesa che Alessandro confermi dal
  vivo che "hey jarvis" funziona col microfono giusto.

## 14:20 — Diagnosi wake word: "hey jarvis" non riconosce la sua voce
- Fatto: Alessandro ha riportato che "hey jarvis" non si attiva, e ha chiesto
  se "ciao jarvis" potesse funzionare. Verificato dal vivo (3 test, con
  probe dedicato che legge il microfono reale e stampa punteggio +
  volume): microfono cattura audio vero (picco RMS 4700-5500, non
  silenzio), ma il punteggio del modello "hey_jarvis" resta 0.002-0.018 —
  ordini di grandezza sotto la soglia (0.5), sia a velocità normale sia
  scandito lentamente. Non è un bug di configurazione: il modello
  preaddestrato semplicemente non riconosce la sua voce/accento su questa
  frase. "Ciao jarvis" non può funzionare per definizione: il modello è un
  classificatore acustico chiuso solo sulla frase inglese "hey jarvis", non
  un sistema a vocabolario aperto.
  Controllata la scorciatoia più leggera di openWakeWord (verificatore
  speaker-dependent, `openwakeword/custom_verifier_model.py`): richiede che
  il modello base superi la soglia su ALMENO alcuni frame delle clip di
  riferimento per estrarne le feature — con punteggi 0.002-0.018 non
  troverebbe nessun frame utilizzabile, quindi non è applicabile qui.
  L'alternativa restante è un modello acustico nuovo da zero (dati
  sintetici TTS multi-voce + augmentation con dataset di rumore/eco) —
  pipeline pesante (`openwakeword/train.py`, richiede torch/torchinfo/
  torchmetrics + download di dataset di rumore di diversi GB), non
  qualcosa da avviare al volo.
- Esito: parziale/informativo. Presentate ad Alessandro 3 opzioni (trigger
  manuale bottone/hotkey, training completo pesante, tenere "hey jarvis"
  così com'è) — ha scelto di **tenerlo così com'è per ora**, nessuna azione
  ulteriore richiesta.
- Imparato: per verificare rapidamente se un wake word è il problema o è il
  microfono, un probe che stampa SIA il punteggio del modello SIA il RMS
  del segnale in tempo reale separa i due problemi in un colpo solo (RMS
  alto + score basso = mic ok, voce non riconosciuta). Il "verificatore"
  speaker-dependent di openWakeWord è utile solo per casi borderline
  (punteggi vicini alla soglia), non quando il modello base non risponde
  affatto.
- Prossimo passo: nessuno per ora. Se Alessandro cambia idea, le due
  strade sono pronte da valutare: trigger manuale (immediato) o training
  custom completo (pesante, da pianificare come blocco a sé).

## 14:45 — Fix bug BrowserAgent + trovata causa reale della lentezza (MCP TradingView)
- Fatto: Alessandro ha riportato l'errore reale "BrowserContext.new_page:
  Target page, context or browser has been closed" durante un comando
  YouTube, e si è lamentato di lentezza generale (chat e daemon vocale).
  Bug browser: `core/browser.py::_ensure_context()` riusava
  `self._context` anche quando il contesto/browser era stato chiuso
  dall'esterno (finestra chiusa a mano, crash) — non c'era controllo.
  Aggiunto `_reset()`/`_new_page()`: su eccezione in `new_page()` ricrea
  playwright+contesto da zero e riprova una volta. Verificato dal vivo
  (chiusura del contesto simulata a metà test, la richiesta successiva si
  riprende da sola).
  Lentezza: misurato un `claude -p "pong"` nudo (~9.6s reale, ttft 5.4s) e
  poi uno realistico con system prompt JARVIS (23s, num_turns:2 — un turno
  extra dovuto a "leggi sempre memory/profile.md" imposto da CLAUDE.md).
  Confronto diretto con/senza `--strict-mcp-config`: **16.5s vs 8.2s**,
  quasi dimezzato. Causa: il server MCP TradingView è configurato a
  livello globale (`~/.claude.json`) e si connette ad ogni singola
  invocazione di `claude -p`, anche fuori dal workspace "trading" dove non
  serve a nulla. Fix in `core/claude_bridge.py::run_claude()`: aggiunto
  `--strict-mcp-config` quando `ws != "trading"` (il workspace trading lo
  mantiene per poter usare davvero gli strumenti TradingView). Verificato
  end-to-end con `run_claude()` reale: 23s → 12.9s (-45%) sulla stessa
  domanda. Bridge riavviato per applicare entrambi i fix.
- Esito: ok, entrambi verificati con dati reali (non mock).
- Imparato: i server MCP configurati a livello utente (non di progetto)
  si caricano per OGNI invocazione di `claude -p` di qualunque bridge,
  indipendentemente dal workspace — vale la pena tenerlo a mente se in
  futuro si aggiungono altri MCP server "pesanti all'avvio" (Google
  Drive/Calendar ecc. dal piano v2): scoparli col workspace giusto invece
  di lasciarli sempre attivi, come già fatto qui per TradingView.
  Riguardo al reclamo su "hey jarvis ci mette un'ora prima di capire": è
  quasi certamente lo STESSO problema già diagnosticato (punteggio
  ~0.002-0.018, mai sopra soglia) — non è lento, per lo più non riconosce
  affatto, e quando per caso/rumore ambientale supera la soglia sembra
  "metterci un'ora" perché nel frattempo si sono accumulati molti
  tentativi falliti. Il fix di oggi (MCP) accelera comunque il giro
  completo una volta che la wake word scatta, ma non risolve il
  riconoscimento in sé — restano valide le due strade già proposte
  (trigger manuale o training custom).
- Prossimo passo: nessuno bloccante. Restano aperte le stesse due strade
  per il riconoscimento vocale, se Alessandro vuole riprenderle.

## 15:10 — Diagnosi finale wake word (canali array esclusi) + hotkey manuale
- Fatto: Alessandro ha insistito che "hey jarvis" non lo riconosce ancora.
  Ultimo test prima di cambiare approccio: catturati e confrontati tutti e
  4 i canali dell'array Intel separatamente (ipotesi: magari leggevo un
  canale non beamformato). Scoperto che i 4 canali riportano lo STESSO
  identico segnale (RMS 3299 identico su tutti) — non c'è un canale
  "giusto" da scegliere, e anche la media tra canali resta a punteggio
  0.01. Confermato in modo definitivo: non è device, non è canale, non è
  velocità/soglia — il modello preaddestrato non riconosce la sua voce su
  questa frase, e ulteriori test non avrebbero cambiato l'esito.
  Presentate di nuovo le due strade (trigger manuale vs training custom);
  Alessandro ha scelto il **trigger manuale**.
  Implementato: hotkey globale **Ctrl+Alt+J** (libreria `keyboard`,
  funziona senza permessi admin su questa macchina) che avvia lo stesso
  identico ciclo della wake word (non lo sostituisce — restano entrambi
  attivi). `core/voice/wake_word.py::WakeWordListener.wait()` ora accetta
  un `cancel_event` opzionale: esce anche se scatta l'evento esterno, non
  solo se il modello rileva la frase o scade il timeout.
  `core/voice/daemon.py::main()`: registra `keyboard.add_hotkey(HOTKEY,
  ...)` e passa l'evento a `listener.wait(cancel_event=...)`.
  `JARVIS_HOTKEY` (env, default `ctrl+alt+j`) in `.env.example`.
- Bug trovato durante la verifica end-to-end (mai emerso prima perché il
  ciclo completo non era mai arrivato fin qui dal vivo — il riconoscimento
  vocale falliva sempre prima): `core/voice/tts.py::EdgeTTSEngine.speak()`
  crashava il daemon INTERO con `PermissionError: [WinError 32]` quando
  provava a cancellare l'mp3 temporaneo subito dopo la riproduzione — su
  Windows il file restava bloccato perché `_decode()` non chiudeva mai
  esplicitamente il container PyAV. Fix: `container.close()` esplicito in
  un `finally` dentro `_decode()`, più un `try/except OSError` attorno al
  cleanup finale come rete di sicurezza. Aggiunto anche un `try/except`
  attorno a tutto il corpo del loop in `daemon.py::main()`: un errore in
  un singolo ciclo ora viene loggato e ignorato invece di far morire tutto
  il processo — prima un bug del genere richiedeva un riavvio manuale
  completo per tornare in ascolto.
  Verificato dal vivo, due volte: hotkey simulata da un processo esterno
  (`keyboard.send('ctrl+alt+j')`) → daemon rileva l'attivazione, registra
  audio ambientale, lo trascrive (whisper ha inventato "Buon appetito!" e
  poi "bla bla bla" dal rumore di fondo — comportamento noto, non un bug),
  spinge un task vero, riceve una risposta reale da Claude, la pronuncia
  ad alta voce SENZA crashare, e torna in ascolto.
- Esito: ok, tutto verificato con dati reali. Daemon riavviato con tutti i
  fix.
- Imparato: il ciclo vocale completo (wake→registra→trascrive→task→
  risposta→parla) non era MAI stato eseguito end-to-end dal vivo prima
  d'ora, perché il riconoscimento della wake word aveva sempre fallito
  prima di arrivarci — per questo il bug di `tts.py` è rimasto nascosto
  per tutto il blocco (c2). Lezione generale: un test "a metà" (sintetico,
  o che si ferma al primo stadio) può dare falsa sicurezza sugli stadi
  successivi mai realmente esercitati.
- Prossimo passo: nessuno bloccante. Alessandro può ora attivare JARVIS a
  voce con Ctrl+Alt+J in ogni momento, indipendentemente dal riconoscimento
  di "hey jarvis".

## 15:40 — Ulteriore riduzione latenza voce + risposta garantita ad ogni attivazione
- Fatto: Alessandro ha chiesto se la voce potesse rispondere "istantaneamente"
  e di rispondere "ad ogni domanda". Aggiunto `VOICE_MODEL` (default
  "haiku") in `core/claude_bridge.py`: il canale `voice` ora usa sempre un
  modello più leggero (testo/Telegram restano sul modello di default),
  giustificato dal fatto che le risposte vocali sono già vincolate a
  massimo due frasi dalla persona. Aggiunta anche una riga alla persona
  che dice di non leggere memory/profile.md per la voce (la memoria
  second-brain rilevante è già iniettata inline).
  Verificato con `run_claude(channel='voice')` reale: 23s (test
  precedente, sonnet + MCP) → 11-12s. Controllato via `num_turns` che il
  turno extra di lettura file resta comunque a 2: è imposto da
  `CLAUDE.md` stesso ("leggi sempre memory/profile.md prima di ogni
  task", marcato come regola che sovrascrive il comportamento di
  default) — la mia nota nella persona non basta a scavalcarlo, e non ho
  toccato CLAUDE.md perché è una regola esplicita sua, non mia da
  rimuovere in autonomia.
  Per "rispondere ad ogni domanda": in `core/voice/daemon.py`, quando la
  trascrizione risulta vuota (silenzio/rumore non riconosciuto) il daemon
  ora dice comunque "Non ho sentito bene, Signore. Mi ripeta pure." invece
  di restare muto senza alcun segnale — prima, un'attivazione senza parlato
  chiaro spariva nel nulla senza che Alessandro sapesse se JARVIS l'avesse
  anche solo sentito.
- Esito: ok, verificato con dati reali (non mock). Bridge e daemon
  riavviati con tutte le modifiche di oggi.
- Imparato: "istantaneo" non è raggiungibile con l'architettura attuale
  (un intero processo CLI `claude -p` per messaggio) — il pavimento fisico
  è di alcuni secondi (avvio CLI + primo token del modello), anche con
  ogni ottimizzazione di configurazione possibile. Per scendere sotto
  quella soglia servirebbe cambiare architettura (API diretta invece del
  CLI), non ancora fatto.
  Probabile causa reale del "non risponde ad ogni domanda": prima del fix
  di oggi su `tts.py`, un crash del daemon a metà sessione lo lasciava
  morto silenziosamente finché non veniva riavviato a mano — ogni domanda
  successiva restava senza risposta. Con la protezione try/except già
  aggiunta oggi nel loop principale, questo scenario specifico dovrebbe
  essere già coperto.
- Prossimo passo: se Alessandro vuole ancora più velocità sulla voce,
  l'unica strada residua è rilassare la regola "leggi sempre
  memory/profile.md" di CLAUDE.md specificamente per il canale voce — da
  chiedere esplicitamente, è una sua regola non mia.

## 16:20 — Bug reale: race condition nel daemon dopo aver parlato
- Fatto: Alessandro ha riportato "non sente più, non trascrive, sembra che
  il microfono non funzioni". Verificato subito con un test RMS live: il
  microfono cattura il suo audio perfettamente (picco 3671, ben sopra
  soglia) — non è un guasto hardware. Controllando il log del daemon si
  vedeva un'attivazione rimasta bloccata su "ascolto..." senza mai
  proseguire. Causa reale trovata in `core/voice/daemon.py::
  _speak_with_interrupt()`: dopo aver parlato, il thread di sottofondo che
  ascolta un'eventuale interruzione resta agganciato al `WakeWordListener`
  (stesso modello openWakeWord, non thread-safe) fino a
  `INTERRUPT_WATCH_SECONDS` (15s) — ma il loop principale procede subito
  dopo a chiamare `listener.wait()` sulla STESSA istanza per la prossima
  attivazione. Due usi concorrenti dello stesso modello/stream si
  bloccano a vicenda. Fix: il watcher ora riceve un `cancel_event` (già
  supportato da `wait()` da prima di oggi) che viene settato subito dopo
  che `engine.speak()` finisce, seguito da un `watcher.join(timeout=2)` —
  garantisce che il thread di sottofondo abbia chiuso il suo stream
  PRIMA che il loop principale riprenda ad ascoltare.
  Verificato dal vivo con due attivazioni consecutive via hotkey subito
  dopo una risposta parlata: la seconda non si è più bloccata, completata
  normalmente. Daemon riavviato con il fix.
- Esito: ok, verificato con dati reali.
- Imparato: quando un oggetto (qui `WakeWordListener`, che incapsula un
  modello openWakeWord stateful) viene condiviso tra il thread principale
  e un thread di sottofondo, un `cancel_event`/`join()` esplicito alla
  fine dell'uso temporaneo è necessario — un `daemon=True` sul thread non
  basta a evitare la sovrapposizione, previene solo che il processo non si
  chiuda per colpa sua.
  Nota anche una particolarità innocua osservata: la primissima
  trascrizione dopo un riavvio del daemon impiega di più (~35s totali)
  perché il modello whisper "small" viene caricato in memoria alla prima
  vera chiamata (lazy loading in `stt._get_model()`) — non un bug, solo un
  costo one-time da comunicare se Alessandro nota lentezza solo sulla
  prima interazione dopo un riavvio.
- Prossimo passo: nessuno bloccante. Se il blocco si ripresenta ancora
  (improbabile ma da tenere d'occhio), il prossimo sospetto sarebbe lo
  stesso pattern di condivisione non thread-safe altrove nel loop.

## 17:00 — Webcam nativa da voce, battuta occhiali, bug encoding critico, finestre ridimensionabili
- Fatto: Alessandro ha chiesto se potesse dire "apri la webcam e dimmi
  cosa vedi" a voce, e che se indossa occhiali JARVIS lo prenda in giro
  dicendo che sembra napoletano. Implementato `core/voice/camera.py`
  (OpenCV, `capture_frame_b64()` + `wants_camera()` per rilevare l'intento
  dal testo trascritto) — riusa lo stesso canale `image_b64` già costruito
  per la dashboard, `daemon.py` cattura un frame vero quando il comando lo
  richiede e lo passa a `_push_task`. Aggiunta l'istruzione scherzosa in
  `core/claude_bridge.py` quando arriva un'immagine. Verificato end-to-end
  con la webcam reale: frame catturato, inviato a Claude via
  `run_claude(channel='voice')`, risposta reale con la battuta sugli
  occhiali.
- Poi Alessandro ha riportato: microfono che sembra non "recepire" più,
  audio parlato non sentito, un caso da "mezz'ora" senza risposta vocale.
  Diagnosi: il microfono cattura audio vero (RMS confermato), ma il log
  del daemon non mostrava NESSUNA attivazione dopo il riavvio per la
  webcam — mistero fino a un test hotkey dal vivo, che ha rivelato il bug
  vero: **su Windows, `print()` su testo con caratteri non-ASCII
  (trascrizioni con em-dash o accenti) lanciava `UnicodeEncodeError`**,
  presa dal try/except del loop — l'intero ciclo falliva in silenzio,
  zero task, zero risposta, zero traccia per l'utente (solo un log criptico
  con "�" ovunque, che era lo stesso sintomo mai preso sul serio prima).
  Fix: `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` in
  cima a `daemon.py`. Verificato: l'em-dash nel banner di avvio ora si
  vede correttamente invece di "�", e un ciclo hotkey completo non crasha
  più.
  Verificato ANCHE oggettivamente che l'audio in uscita funzioni:
  catturato in loopback via device "Stereo Mix" mentre `tts.speak()`
  parlava — segnale reale forte (RMS ~2000 medio, picchi ~12500). Il
  software produce audio vero; se Alessandro non lo sente è un problema
  di device/volume Windows, non del codice. Aggiunto comunque
  `JARVIS_SPEAKER_DEVICE` (stesso pattern di `JARVIS_MIC_DEVICE`, ma per
  l'uscita) in `core/voice/__init__.py::resolve_output_device()` +
  `tts.py`, come rete di sicurezza se il device di default cambia.
- Infine: la finestra second brain nella dashboard non ci stava.
  Aggiunto `resize: both` + `min-width`/`min-height`/`max-width` su
  `.floating-window` (style.css), `flex: 1` su `.fw-body` (altrimenti non
  si espande dentro il contenitore ridimensionato), e `#brain-canvas`
  passato da altezza fissa (340px) a `height: 100%` + `min-height: 340px`.
  Il canvas del grafo non si ridisegnava alla dimensione giusta al resize
  manuale (solo su resize della finestra del BROWSER): aggiunto un
  `ResizeObserver` dedicato in `app.js::setupBrainCanvas()`. Verificato
  parzialmente: confermato via DOM che la finestra si apre con le classi e
  dimensioni giuste e `resize:both` è applicato; un test di drag-resize
  dal vivo nel browser non è stato possibile completarlo (il tab si è
  bloccato più volte durante i test, probabile carico del loop di
  animazione del grafo) — verifica visiva finale lasciata ad Alessandro.
- Fatto anche: popolato il second brain con la conoscenza retroattiva di
  JARVIS + AURA/ConciergeFlow + White Soul Ibiza + TradeFlow-AI (117 nodi,
  91 relazioni, tutti specchiati come note reali in Obsidian, verificato).
  Tre fork paralleli hanno esplorato AURA/ConciergeFlow, White Soul,
  TradeFlow in sola lettura; ho scritto io il payload di JARVIS stesso
  (contesto già in memoria di sessione). **Nota di sicurezza**: il fork
  che ha esplorato White Soul Ibiza ha tentato di cancellare un file di
  stato interno della sessione Claude Code (tool-results) senza
  autorizzazione — segnalato esplicitamente ad Alessandro prima di usare
  il suo output, che nel merito sembrava comunque legittimo. L'inserimento
  ha richiesto 4 tentativi per via di ripetuti timeout di rete transitori
  (stesso pattern di sempre su questa macchina); reso lo script idempotente
  con retry automatico per l'ultimo giro, che ha chiuso da solo.
- Esito: ok su tutti i punti verificabili (webcam, encoding, audio in
  uscita, second brain). Resize finestra da confermare visivamente da
  Alessandro.
- Imparato: i caratteri "�" comparsi in OGNI log di oggi non erano solo
  cosmetici — erano il sintomo visibile di un problema di encoding che,
  in certe condizioni, diventava un crash silenzioso vero. Vale la pena
  controllare l'encoding di stdout/stderr per primo, non per ultimo, la
  prossima volta che un processo Windows sembra "perdere" input senza una
  causa ovvia.
- Prossimo passo: Alessandro deve confermare dal vivo che il
  ridimensionamento delle finestre funziona visivamente come atteso.

## 18:00 — Comandi OS reali (apri/chiudi/volume/blocco/power), fix webcam, riconoscimento volto, grafo completo
- Fatto: Alessandro ha segnalato 6 problemi in un colpo solo: risposte
  vocali lente, "chiudi Chrome" (e controllo OS in generale) non esiste,
  "apri la webcam" apre per sbaglio Chrome su localhost invece del
  riquadro camera, vuole riconoscimento volto in camera, vuole risposte
  vere come una IA reale (non solo battute/frasi tagliate), non capisce
  perche' il microfono sembra non rispondere appena il daemon parte.
  A meta' turno, aggiunto anche: il grafo second brain mostra solo un
  pezzo, non tutto. Branch `feature/jarvis-v3-os-control`.
  - **Causa radice webcam** trovata leggendo il codice: sia
    `core/voice/camera.py::CAMERA_INTENT_RE` (daemon) sia l'intercettazione
    lato client in `web/public/app.js::handleVoiceUiCommand` (dashboard)
    riconoscevano "camera/telecamera/fotocamera" ma NON la parola
    "webcam" usata da lui, e nel caso del daemon serviva anche un verbo
    tipo "vedi/cosa" — "apri la webcam" da sola non bastava. Risultato:
    la frase cadeva come task generico a Claude, che senza un modo reale
    di "aprire la webcam" improvvisava aprendo un browser. Fix: entrambe
    le regex ora includono "webcam" e verbi di apertura
    (apri/accendi/attiva), non solo domande.
  - **Controllo OS reale** (mai esistito prima per voce/chat, solo
    `/apri` esplicito su Telegram): `core/system_executor.py` esteso con
    `close_app` (psutil, termina per nome processo), `volume`
    (trucco noto SendKeys con codici carattere 173/174/175 per
    mute/giu/su), `lock_workstation`, `show_desktop`, `screenshot`
    (via .NET da PowerShell), `power_action` (shutdown/restart/logoff —
    **sempre** dietro conferma esplicita via lo stesso meccanismo
    `_stage_confirmation`/token gia' usato per `run`). `APP_REGISTRY`
    esteso con chrome/edge/spotify/calcolatrice/paint/terminale/cmd/
    powershell/task manager.
    Nuovo `core/intents.py`: riconosce frasi BREVI (max 10 parole) tipo
    "apri chrome"/"chiudi vs code"/"alza il volume"/"blocca lo
    schermo"/"spegni il pc" e le esegue SUBITO, senza passare da Claude —
    zero costo, zero latenza di un giro `claude -p`. Frasi piu' lunghe o
    composte non vengono intercettate (restano a Claude). Per le
    richieste composte che intents.py lascia passare, nuovo
    `core/system_actions.py` (stesso pattern di `core/browser.py`):
    Claude puo' emettere un blocco ` ```system``` ` che esegue la stessa
    azione via `SystemExecutor` — documentato nel `SYSTEM` di
    `core/claude_bridge.py`, insieme a un'esclusione esplicita ("non
    usare mai il blocco browser per la webcam o il second brain, sono
    pannelli locali"). Spegnimento/riavvio/logout dal canale **vocale**
    sono rifiutati del tutto (nessun flusso di conferma sensato a voce,
    redirect a Telegram/dashboard) — solo testo/Telegram/dashboard
    possono davvero avviarli, e comunque solo dopo `/confirm`.
    Estratto `core/executor_singleton.py` (istanza condivisa, evita un
    import circolare con `claude_bridge`) — usato da `bot.py`,
    `core/web_bridge.py`, `core/voice/daemon.py`.
  - **Riconoscimento volto**: nuovo `core/voice/face_id.py`, OpenCV LBPH
    (`cv2.face`, richiede `opencv-contrib-python` — sostituito
    `opencv-python`, che non ha il modulo `face`) + Haar cascade per la
    detection. Non e' un sistema di sicurezza (facilmente ingannabile con
    una foto), solo personalizzazione: se riconosce Alessandro con
    confidenza sufficiente lo dice a Claude nel prompt, che puo'
    rivolgersi a lui per nome. Comando Telegram `/enroll_face` cattura
    ~20 frame dalla webcam e allena il modello (dati in `.face_data/`,
    gitignored — biometrico, non codice). Wired dentro
    `claude_bridge.run_claude()`: copre SIA la webcam del daemon vocale
    SIA quella della dashboard (stesso punto di ingresso `image_b64`).
    **Scoperta inaspettata**: la wheel pip installata di
    `opencv-contrib-python` (5.0.0.93) NON include i file XML delle Haar
    cascade in `cv2/data/` (cartella presente ma vuota,
    `cv2.data.haarcascades` punta a una directory senza i file) — cascade
    scaricata dal repo ufficiale OpenCV e committata in
    `core/voice/data/haarcascade_frontalface_default.xml` invece di
    dipendere dal pacchetto pip. Da tenere a mente se in futuro un
    aggiornamento di opencv rompe di nuovo la stessa cosa.
  - **Velocita' voce**: `core/voice/stt.py::warm_up()` forza il
    caricamento del modello whisper all'avvio del daemon invece che alla
    prima trascrizione reale (~30s la prima volta, gia' notato alle
    16:20 come "innocuo" ma probabile causa reale del "non mi sente
    appena parte" segnalato oggi). I comandi rapidi (intents.py) ora
    rispondono senza nessun giro `claude -p`. Il pavimento per le
    risposte vere resta quello gia' documentato alle 15:40 (CLI
    `claude -p` per messaggio, ~11-12s) — non toccato oggi, servirebbe
    passare dall'API diretta invece del CLI, cambio architetturale non
    fatto.
  - **Persona vocale**: `core/persona.py` non impone piu' un tetto rigido
    di due frasi a tutto — resta una frase sola per le conferme d'azione,
    ma le domande vere ora possono avere una risposta completa e
    naturale (l'obiettivo era "che risponda come una IA vera", non solo
    battute brevi). Aggiunto anche il divieto esplicito di markdown/liste
    puntate nella voce (si sente ad alta voce, non si legge).
  - **Grafo second brain**: `web/public/app.js::brainRender()` disegnava
    a scala 1:1 senza adattarsi al canvas — con 117 nodi il layout a
    forze li spinge ben oltre i bordi visibili, quindi si vedeva solo il
    cluster centrale. Aggiunto zoom-to-fit automatico (bounding box dei
    nodi ricalcolata ogni frame, scala di conseguenza, mai sopra 1x) +
    aggiornate le coordinate di hover/click/doppio click per tenere
    conto della stessa scala.
  - Test: aggiunti `tests/test_intents.py` (parsing puro, nessun
    side-effect reale) e nuovi casi in `tests/test_system_executor.py`
    per `close_app`/`volume`/`power_action` (mockati, NON eseguiti
    davvero sulla macchina — spegnimento/volume/lock reali non sono cose
    da testare dal vivo senza che lui lo sappia). 24/24 test passano.
- Esito: ok per tutto cio' che si poteva verificare senza toccare
  azioni reali sulla sua macchina (import puliti, regex verificate con
  casi reali, cascade Haar caricata correttamente, test automatici
  verdi). NON verificato dal vivo (richiede lui): che "chiudi chrome"/
  "alza il volume"/ecc. funzionino davvero parlando o scrivendo sul suo
  PC; che `/enroll_face` catturi davvero il suo volto e che il
  riconoscimento successivo lo identifichi; che il grafo second brain
  ora si veda per intero nel browser vero.
- Imparato: quando un progetto ha piu' punti d'ingresso paralleli per lo
  stesso tipo di comando (voce, Telegram, dashboard web), un bug di
  riconoscimento testuale (qui: manca "webcam" nel vocabolario) va
  cercato e corretto in OGNI punto d'ingresso separatamente — non basta
  fixare il daemon se il problema si ripresenta identico nel client-side
  JS della dashboard. Le wheel pip di OpenCV possono cambiare cosa
  impacchettano tra una versione e l'altra (qui: niente piu' Haar
  cascade in `cv2/data/`) — per una dipendenza critica come la cascade di
  detection, committare il file nel repo e' piu' robusto che fidarsi del
  pacchetto pip.
- Prossimo passo: Alessandro deve provare dal vivo i nuovi comandi
  (voce + Telegram + dashboard), fare `/enroll_face` almeno una volta, e
  confermare il grafo. Se vuole ancora piu' velocita' sulla voce, l'unica
  strada resta quella gia' nota (API diretta invece del CLI `claude -p`,
  cambio architetturale piu' grosso, da pianificare a parte).

## 19:15 — Voce su API Anthropic diretta (non piu' claude -p via coda Turso)
- Fatto: Alessandro ha chiesto di riavviare bot.py + daemon vocale, poi di
  passare subito il canale vocale all'API diretta (cambio gia' anticipato
  poche righe sopra come "unica strada residua"). Confermato con lui
  esplicitamente prima di procedere: SOLO il canale vocale (Telegram/
  dashboard restano su `claude -p`, servono gli strumenti file/bash per i
  task reali), e second brain in sola lettura per la voce (niente scrittura
  di nodi nuovi da quel canale).
  Nuovo `core/claude_api.py`: `anthropic.AsyncAnthropic` diretto, modello
  `claude-haiku-4-5` (stessa scelta gia' fatta per la voce via CLI, stesso
  motivo — risposte gia' vincolate a poche frasi dalla persona), system
  prompt = `claude_bridge.SYSTEM` + contesto second-brain (sola lettura,
  `brain.fetch_context`) + `persona.PERSONA`. Immagini passate come content
  block nativo (base64 diretto, niente file temporaneo ne' "usa il tuo
  Read tool" — l'API vede l'immagine per davvero senza bisogno di
  strumenti). Riconoscimento volto invariato (decodifica il frame in
  memoria invece che da file). Cronologia conversazione tenuta in memoria
  di processo (dict per workspace), NON in `state.json`: persisterla
  avrebbe creato una corsa tra il processo del daemon e quello di bot.py
  che scrivono sullo stesso file — si azzera ad ogni riavvio del daemon,
  scelta accettata per un canale effimero per natura. I blocchi
  ` ```system``` `/` ```browser``` ` restano processati identicamente
  (sono estrazione di testo via regex, non serve nessuno strumento nativo
  di Claude per farli funzionare).
  `core/voice/daemon.py` riscritto: niente piu' `_push_task`/`_poll_task`
  (insert 'pending' + polling su Turso ogni 1.5s fino a 3 minuti) — ora
  chiama `claude_api.run_voice()` direttamente e basta. Resta un solo
  INSERT con stato gia' 'done' nella tabella `tasks` dopo il fatto, solo
  per non far sparire le interazioni vocali dal contatore/storico task
  della dashboard (`tasks_recent` in `web/public/app.js`) — non e' piu'
  una coda, e' un log a cose fatte. Gestione errori dedicata: un
  `TypeError` con "authentic" nel messaggio (l'SDK Anthropic lo solleva
  quando non trova NESSUNA credenziale configurata, PRIMA di qualunque
  chiamata di rete — diverso da `anthropic.AuthenticationError`, che e' un
  401 vero dal server per una chiave presente ma invalida) fa rispondere
  a voce con un messaggio chiaro invece di un errore criptico.
  Bot e daemon riavviati con tutto il codice della sessione (fix webcam,
  intents, riconoscimento volto, API diretta compresi). Committato su
  branch `feature/jarvis-voice-direct-api`, mergiato e pushato su main.
- Esito: parziale, dichiarato onestamente. Codice verificato (import puliti,
  27/27... anzi 24 test passano, `TypeError` di autenticazione riprodotto e
  gestito correttamente in una prova dal vivo senza chiave). **Manca
  `ANTHROPIC_API_KEY` nel `.env`**: l'autenticazione del CLI `claude`
  (login/abbonamento) NON vale per l'SDK Python diretto, che ha un sistema
  di credenziali completamente separato — servono una chiave vera da
  console.anthropic.com e la fatturazione a consumo che ne consegue
  (verosimilmente diversa da qualunque piano copra gia' le chiamate
  `claude -p`). Chiesto esplicitamente ad Alessandro di incollarla in
  chat, coerente con come gestisce gia' gli altri segreti del progetto.
  Finche' non arriva, il daemon risponde a voce "manca la chiave API" in
  modo chiaro invece di restare muto o crashare.
- Imparato: l'SDK `anthropic` python non eredita in alcun modo
  l'autenticazione del `claude` CLI (OAuth/abbonamento) — sono due sistemi
  di credenziali indipendenti, anche se entrambi "di Anthropic". Va
  sempre verificato esplicitamente prima di promettere che un cambio del
  genere "funziona subito". L'SDK solleva un `TypeError` semplice (non
  una sua eccezione dedicata) quando non trova nessuna credenziale — va
  intercettato separatamente da `anthropic.AuthenticationError`.
- Prossimo passo: Alessandro deve incollare `ANTHROPIC_API_KEY` — a quel
  punto il canale vocale e' pienamente operativo sull'API diretta. Poi
  provare dal vivo tutti i punti ancora aperti di oggi (comandi OS,
  `/enroll_face`, grafo, e ora anche la latenza vocale reale con l'API).

## 19:40 — Chiave Anthropic aggiunta, canale vocale API diretta verificato dal vivo
- Fatto: Alessandro ha creato la chiave su console.anthropic.com e l'ha
  incollata in chat. Scritta in `.env` come `ANTHROPIC_API_KEY` (mai
  ripetuta in chiaro nelle mie risposte). Verificata con una chiamata
  reale minima (risposta "ok"), poi riavviato il daemon vocale per
  ricaricare il nuovo `.env`.
- Esito: ok, verificato end-to-end con una chiamata vera (non mock) a
  `claude_api.run_voice()`: "quanto fa 12 per 8?" -> "Novantasei,
  Signore." (persona applicata correttamente, contesto second-brain
  iniettato senza errori), costo stimato ~$0.006. Il canale vocale e' ora
  pienamente operativo sull'API diretta.
- Prossimo passo: nessuno bloccante. Restano da confermare dal vivo (con
  la sua voce reale, non un test da terminale): la latenza percepita
  effettiva con l'API diretta, i comandi OS rapidi, `/enroll_face`, e il
  grafo second brain nella dashboard.

## 20:50 — Bug reali dal primo uso vero: apri chrome, webcam via browser (di nuovo), ora sbagliata
- Fatto: Alessandro ha incollato un log reale di dashboard/Telegram con 3
  problemi. Diagnosticati e risolti tutti e tre:
  1. **"apri chrome" -> WinError 2**: causato da me poche ore fa. Avevo
     aggiunto `"chrome": "chrome.exe"` (ecc. per Edge/Spotify) in
     `APP_REGISTRY`, ma quegli eseguibili — a differenza di
     notepad/calc/cmd (System32, sempre sul PATH) — NON sono sul PATH di
     Windows: si installano altrove e si registrano solo nel registro
     "App Paths", che `_resolve_app_path()` gia' sapeva interrogare PRIMA
     della mia modifica. Mettendoli in `APP_REGISTRY` la risoluzione
     dinamica veniva bypassata. Rimossi da `APP_REGISTRY` (restano solo
     notepad/explorer/calc/paint/cmd/powershell/taskmgr, verificati
     System32), aggiunto `DYNAMIC_APP_NAMES` — solo vocabolario per
     `core/intents.py`, la risoluzione vera passa sempre da
     `_resolve_app_path`. Verificato dal vivo: `chrome`/`msedge` ora
     risolvono al path reale installato su questa macchina.
  2. **Webcam via browser, di nuovo**: "scatta e dimmi cosa vedi" (senza
     la parola "webcam") non veniva intercettato da NESSUN canale
     testuale — `camera.wants_camera()` viene chiamato solo dentro
     `core/voice/daemon.py`, mai da `bot.py`/`web_bridge.py`. Claude,
     senza immagine e senza modo di "accendere" la webcam, ha
     improvvisato aprendo un browser verso `http://localhost:3000/`
     (stesso bug della mattina, ricomparso per una frase diversa non
     coperta dal fix precedente). Fix su due livelli: (a) aggiunta la
     STESSA cattura nativa (`camera.capture_frame_b64`) anche in
     `bot.py` e `core/web_bridge.py`, solo quando il client non ha gia'
     allegato un'immagine — ora QUALUNQUE canale (voce, Telegram,
     dashboard) puo' davvero scattare una foto reale dalla webcam del
     PC; (b) `core/voice/camera.py::CAMERA_INTENT_RE` ora riconosce anche
     "scatta/fotografa/fai una foto" da sole, non solo abbinate alla
     parola "webcam" (accettato un piccolo rischio di falso positivo, es.
     "quando scatta l'allarme" — coerente col resto del progetto); (c)
     riscritta l'istruzione nel `SYSTEM` prompt in modo generale ("non hai
     NESSUN modo di accendere la webcam, mai aprire un browser per
     rimediare") invece di ancorarla al solo esempio letterale "apri la
     webcam", che non generalizzava a frasi diverse.
  3. **"che ore sono" -> non lo sapeva**: nessuna data/ora reale veniva
     mai iniettata nel prompt (ovvio con il senno di poi). Aggiunta
     `datetime.now()` fresca ad ogni chiamata sia in
     `core/claude_bridge.py` sia in `core/claude_api.py` (due system
     prompt costruiti separatamente, serviva in entrambi). Verificato dal
     vivo: "che ore sono?" -> "Sono le 20:47, Signore." (corretto).
  Bot e daemon riavviati con tutti i fix. Branch
  `fix/jarvis-open-app-webcam-datetime`, mergiato e pushato su main.
- Esito: ok, tutti e tre verificati con chiamate reali (non mock):
  risoluzione path di chrome/edge sul registro vero di questa macchina,
  regex webcam testata su tutte le frasi del log incollato, chiamata
  API reale che ora risponde l'ora corretta. 24/24 test passano.
- Imparato: quando si aggiunge un nome-app a un vocabolario di
  riconoscimento (qui `APP_REGISTRY`), va sempre verificato che
  l'eseguibile sia DAVVERO raggiungibile con un Popen a nome nudo — solo
  gli eseguibili in `System32` lo sono per garanzia, tutto il resto
  (Program Files, AppData, install per-utente) va risolto dinamicamente.
  Un'istruzione nel system prompt ancorata a un ESEMPIO letterale
  ("apri la webcam") non generalizza automaticamente a frasi diverse con
  la stessa intenzione ("scatta una foto") — meglio dare a Claude i DATI
  di cui ha bisogno (l'immagine vera, catturata prima di chiamarlo) che
  contare solo su un'istruzione per impedirgli di improvvisare.
- Prossimo passo: nessuno bloccante. Alessandro puo' riprovare dal vivo
  gli stessi comandi che avevano fallito.
