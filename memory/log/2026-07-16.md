## 16:07 — Meteo, log di ogni interazione nel second brain, hardening Turso, check TradeFlow

- Fatto: Alessandro ha chiesto JARVIS "come un sistema operativo" con alcuni
  esempi (webcam + battuta occhiali, che ore sono, che meteo c'è, check
  TradeFlow) più due richieste trasversali: analisi approfondita con fix di
  tutto ciò che non funziona, e che ogni interazione/domanda aggiorni il
  grafo second brain/Obsidian. Riletta tutta la storia recente (memory/log
  13-15/07): webcam+battuta napoletano, controllo OS, riconoscimento volto e
  "che ore sono" erano GIÀ implementati (branch `feature/jarvis-v3-os-control`
  e `fix/jarvis-open-app-webcam-datetime`, già in main) — verificato che il
  codice è ancora tutto presente e coerente, non ricostruito da zero. Il
  meteo non esisteva affatto. "Ogni interazione aggiorna il grafo" non era
  vero: il second brain si scrive solo quando Claude sceglie di emettere un
  blocco ```brain```, MAI per i comandi rapidi di `core/intents.py` (zero
  costo/latenza, non passano da Claude), e il canale vocale (`core/claude_api.py`)
  non scriveva mai nodi per decisione esplicita del 2026-07-15.
  Branch `feature/jarvis-weather-brain-logging`:
  - **Nuovo `core/weather.py`**: Open-Meteo (nessuna API key) + geocoding
    città + fallback IP (ip-api.com), cache 20 min (60s in caso di errore, per
    non aggiungere un timeout ad ogni messaggio se la rete è giù). Iniettato
    nel system prompt esattamente come data/ora, sia in `core/claude_bridge.py`
    sia in `core/claude_api.py`. Override opzionale via
    `JARVIS_WEATHER_LAT/LON/CITY` in `.env.example`.
  - **`core/brain.py::log_interaction()`** (nuovo): un nodo per giorno+workspace
    (non uno per domanda, altrimenti il grafo si sommerge di nodi usa-e-getta)
    che bump-a hits/summary ad OGNI interazione — riusa l'upsert già esistente
    di `_store()`. Wired in `core/claude_bridge.py::run_claude()` (canale
    testo/Telegram/dashboard), `core/claude_api.py::run_voice()` (canale
    vocale) e `core/intents.py::execute_intent()` (comandi rapidi — un solo
    punto d'ingresso condiviso da bot.py/web_bridge.py/daemon.py, che ora
    passano `workspace`/`raw_text`).
  - **Bug reale corretto in `core/claude_api.py`**: `run_voice()` non
    chiamava mai `brain.extract_and_store()` — un blocco ```brain``` emesso
    da Claude durante una risposta vocale (il SYSTEM condiviso lo incoraggia
    per qualunque canale) non veniva MAI ripulito, sarebbe finito letto ad
    alta voce come JSON grezzo dal TTS. Approfittato del fix per abilitare
    anche la scrittura (la decisione "sola lettura" del 2026-07-15 è superata
    dalla richiesta di oggi).
  - **Bug reale corretto in `core/brain.py`**: `fetch_context()` e
    `extract_and_store()` chiamavano `_bootstrap()`/Turso SENZA try/except —
    un blip di rete transitorio (pattern documentato e ricorrente su questa
    macchina, vedi memoria [[network-quirks-this-pc]]) durante quella
    chiamata faceva fallire l'INTERA risposta a Claude (errore mostrato
    all'utente anche se Claude aveva già risposto bene), invece di degradare
    silenziosamente come il resto del second brain (arricchimento, non
    requisito). Ora entrambe tornano un default sicuro ("" / testo ripulito)
    su qualunque eccezione.
  - Test: `tests/conftest.py` nuovo (autouse: disabilita Turso reale e il
    vault Obsidian reale nei test — questa macchina ha credenziali vere in
    `.env`, senza guardrail i test avrebbero scritto dati finti nel second
    brain/vault di produzione). `tests/test_weather.py`,
    `tests/test_brain.py` nuovi. Aggiunte a `tests/test_intents.py` per il
    logging. 41/41 test passano (24 esistenti + 17 nuovi).
  - Smoke test REALI (nessun processo bot/daemon attivo, quindi sicuro):
    `weather.get_weather_line()` con rete vera → "32°C, cielo sereno, vento
    12 km/h (Palma)"; `brain.log_interaction()` con Turso vera → riga creata
    in `brain_nodes` E nota reale specchiata in
    `jarvis/SecondBrain/interazioni-jarvis-2026-07-16.md` (frontmatter +
    corpo corretti). Pipeline end-to-end confermata con dati reali, non mock.
  - **TradeFlow AI**: controllato (sola analisi/sanity-check, regola ferrea
    del progetto — nessuna modifica fatta lì). Trovato un bug attivo serio:
    bot fermo dal 2026-07-10 (`_strategy_order_tickets` si blocca sulla
    chiave troncata dal commento MT5, deadlock a `count_open_positions()`
    bloccato a 2 con 0 posizioni reali) — fix già scritto e documentato nel
    loro `directives/06_known_issues.md` ma non deployato, serve anche un
    riavvio del bot sulla VPS. Cron giornaliero fermo da una settimana
    (nessun log dopo il 2026-07-09). Dettagli in
    `memory/projects/tradeflow-ai.md`.
- Esito: ok. Weather e brain-logging verificati con chiamate reali (rete e
  Turso vere), non solo mock. Comandi OS/webcam/voce non ritoccati oggi:
  verificati per lettura del codice (tutto presente e coerente), non
  ri-testati dal vivo in questa sessione — restano nello stato "verificato
  l'ultima volta il 2026-07-15" già in memoria.
- Imparato: quando un modulo second-brain/Turso viene richiamato da PIÙ
  punti d'ingresso (testo, voce, comandi rapidi), conviene centralizzare la
  logica di logging in UN punto condiviso (qui: dentro `execute_intent()`
  stesso, non nei 3 chiamanti) invece di duplicarla — meno punti da tenere
  sincronizzati se la logica cambia ancora. Un test che chiama codice
  collegato a Turso/Obsidian VA sempre schermato con un guardrail esplicito
  (autouse fixture), anche se "tanto nei test normalmente turso.ENABLED è
  false" — su una macchina di sviluppo con `.env` reale non lo è affatto, e
  senza il guardrail i test avrebbero scritto dati di test nel second brain
  e nel vault Obsidian veri di Alessandro (scoperto rileggendo `_vault()` e
  `_bootstrap()` PRIMA di lanciare i test, non dopo).
- Prossimo passo: mergiato in main (vedi sotto). Da provare dal vivo quando
  capita: chiedere "che meteo c'è" a voce/testo e controllare che il grafo
  second brain nella dashboard mostri un nodo "Interazioni ws — data" che
  cresce ad ogni scambio. Per TradeFlow: deployare il fix del bot e
  riavviarlo sulla VPS (fuori dal perimetro di JARVIS, azione sua o di una
  sessione Claude Code dentro quel repo).

## 16:25 — Incidente live: bridge spento + webcam lentissima (bug reale)

- Fatto: Alessandro ha segnalato in chat "se scrivo nella chat non succede
  nulla" mentre testava proprio la funzione webcam dal report delle 16:07.
  Diagnosi immediata: nessun processo Python era in esecuzione (bot.py non
  viene avviato da solo dopo un riavvio/o non era mai stato avviato oggi) —
  i suoi messaggi finivano nella coda Turso come 'pending' e restavano lì
  per sempre, nessuno li reclamava. Avviato `bot.py` (`.venv/Scripts/python.exe`,
  non l'interprete globale — coerente con la lezione già in memoria). Alla
  ripartenza il bridge ha drenato da solo 3 task rimasti in coda da oggi
  (12:08, 14:10, 14:13), tutti con prompt legati alla webcam.
  Uno di questi task ha riprodotto dal vivo un bug reale che Alessandro ha
  poi confermato lui stesso mentre lo investigavo: "apri la webcam e dimmi
  cosa vedi" apriva un browser rotto (`ERR_CONNECTION_REFUSED` su
  `localhost:3000`) invece del vero feed. Causa: `core/voice/camera.py::
  capture_frame_b64()` chiamava `cv2.VideoCapture(device_index)` senza
  backend esplicito — su questa macchina OpenCV sceglie MSMF/auto-detect,
  che impiega **28-56 secondi** per aprire la webcam (misurato dal vivo,
  confrontati DSHOW/MSMF/ANY) invece di ~2.5s con DirectShow. Con
  un'attesa così lunga, Claude talvolta procedeva senza immagine pronta e
  improvvisava aprendo un browser (stesso sintomo del bug "vecchio" già
  fissato il 15/07, ma con una causa radice diversa e più a monte: qui
  l'immagine non arrivava mai in tempo, non mancava il riconoscimento
  testuale dell'intento). Fix: `cv2.VideoCapture(device_index, cv2.CAP_DSHOW)`
  esplicito. Verificato dal vivo: 2.44s, cattura riuscita. Branch
  `fix/jarvis-webcam-dshow-backend`, mergiato e pushato su main. Bridge
  riavviato di nuovo per caricare il fix. 41/41 test ancora verdi (nessun
  test copriva `camera.py` nello specifico — modulo che tocca hardware
  reale, testato dal vivo invece che con mock, coerente con lo stile del
  resto del progetto per questo tipo di codice).
  Alessandro ha chiesto anche perché i comandi testuali sono lenti in
  generale — spiegato che è un problema diverso e già noto: il canale
  testo/dashboard passa sempre da `claude -p` (CLI intero per messaggio,
  ~10-25s) per avere accesso reale a Read/Write/Bash nei workspace, mentre
  la voce è già stata spostata sull'API diretta (~11-12s) proprio per
  questo. Non ho spostato anche testo/dashboard sull'API diretta: è la
  stessa scelta già fatta e discussa esplicitamente il 15/07 (si perderebbe
  l'accesso agli strumenti per i task di sviluppo reali su quel canale) —
  gliel'ho proposta come opzione, non applicata di mia iniziativa.
- Esito: ok, causa radice trovata e risolta per entrambi i problemi
  (bridge spento, webcam lenta), verificato con dati reali (non mock) in
  entrambi i casi.
- Imparato: quando un modulo Python usa una libreria con più backend
  possibili per lo stesso device fisico (qui `cv2.VideoCapture`), NON
  specificare il backend lascia decidere una euristica della libreria che
  può essere ordini di grandezza più lenta di quella esplicita e corretta
  per la piattaforma — vale la pena forzarlo esplicitamente invece di
  fidarsi dell'auto-detect, specialmente per hardware I/O. Un processo di
  sfondo che "deve essere avviato manualmente ad ogni riavvio" (nota già
  in memoria da giorni) continua a essere la causa più probabile e più
  facile da controllare per primo quando "non succede nulla" — va
  verificato SUBITO con un controllo processi, prima di sospettare bug nel
  codice.
- Prossimo passo: Alessandro riprova "apri la webcam e dimmi cosa vedi" dal
  vivo (dovrebbe essere rapido e mostrare il feed vero, con la battuta sugli
  occhiali se li indossa). Da valutare se vuole anche testo/dashboard
  sull'API diretta per la velocità, sacrificando l'accesso agli strumenti
  file/bash su quel canale — non ancora deciso.

## 17:10 — "Super efficienza": --bare, contesto ridotto, log non bloccante

- Fatto: Alessandro ha chiesto un modo per essere piu' veloci SENZA perdere
  gli strumenti (Read/Write/Bash) sul canale testo/dashboard. Investigato
  con misure reali (non stime), non solo l'idea del flag `--bare` proposta
  prima:
  1. **`--bare`** (verificato con `claude -p --help`): salta hook/LSP/
     plugin-sync/auto-memory/prefetch/CLAUDE.md auto-discovery di Claude
     Code. Testato a fondo: strumenti reali confermati (Read su un file
     vero, num_turns=2), `--resume` confermato compatibile (sessione
     multi-turno intatta). Costo per chiamata 5-10x piu' basso in ogni
     prova. **Richiede pero' `ANTHROPIC_API_KEY` esplicita nell'ambiente**
     (un primo giro di test e' fallito con "Not logged in" per un bug del
     MIO script di test, non del flag: `load_dotenv()` senza path esplicito
     cerca risalendo dal file chiamante, non dalla cwd — uno script fuori
     dall'albero del repo non trova mai `.env`). Attivato con
     `JARVIS_CLI_BARE=1` di default (nuovo toggle in `.env.example`, metti
     a 0 per tornare al comportamento precedente). Aggiunti esplicitamente
     al `SYSTEM` di `claude_bridge.py` i "Confini" di CLAUDE.md (mai
     social/trading reale/push su main) perche' `--bare` salta la lettura
     automatica di quel file e quelle regole non erano ancora duplicate a
     mano.
  2. **Trovato un problema piu' grosso mentre misuravo**: il contesto
     second-brain iniettato in OGNI messaggio (`brain.fetch_context`) era
     arrivato a **15+ KB** (40 nodi, summary lunghi) — probabilmente il
     contributo maggiore alla lentezza percepita, non il processo CLI in
     se'. Ridotto `CONTEXT_LIMIT` 40->15 e troncati i summary a 200
     caratteri: stesso contesto ~4-5KB. Escluso anche i nuovi nodi
     "interazione" (log_interaction, aggiunti oggi alle 16:07) dalla query
     di `fetch_context`: il loro hits cresce ad ogni scambio quindi
     avrebbero finito per salire sempre in cima all'ordinamento e soffocare
     i nodi con fatti veri ma piu' rari — restano nel grafo/Obsidian, non
     nel prompt.
  3. **Bug introdotto da me stesso poche ore prima, corretto**:
     `log_interaction()` (il log di ogni interazione, 16:07) era una
     scrittura Turso sincrona (~0.8s) messa in coda PRIMA di rispondere
     all'utente — per i comandi rapidi di `core/intents.py` (pensati per
     "zero latenza") era una regressione diretta. Spostato su un thread a
     parte (non asyncio.to_thread: `core/voice/daemon.py` chiama
     `claude_api.run_voice` via `asyncio.run()` per singola invocazione, un
     task asyncio non atteso verrebbe cancellato alla chiusura del loop
     prima di completare — un thread normale no), fire-and-forget in tutti
     e tre i punti (intents.py, claude_bridge.py, claude_api.py).
  Verificato end-to-end con `run_claude()` reale (non mock), stessa
  domanda ("quanto fa 12 per 8?"), piu' misurazioni: **14-32s -> 7-9s**.
  42/42 test passano (corretti 2 test che sarebbero diventati intermittenti
  per via del thread fire-and-forget, con un finto Thread sincrono nei
  test). Bridge riavviato con tutto caricato.
- Esito: ok, verificato con dati reali (non mock) su ogni singolo punto.
- Imparato: quando si misura la latenza di una pipeline con piu' stadi
  (CLI + contesto iniettato + logging), profilare OGNI stadio
  separatamente prima di concludere quale sia il collo di bottiglia — la
  mia ipotesi iniziale (il flag CLI) non era la causa principale, il
  contesto second-brain lo era, e l'ho scoperto solo strumentando le fasi
  una per una. Una feature aggiunta "per bene" poche ore prima (log di
  ogni interazione) puo' introdurre una regressione di latenza in un punto
  che aveva una garanzia esplicita diversa (zero latenza di intents.py) —
  vale la pena rileggere le garanzie di design originali di un modulo
  prima di agganciarci qualcosa di nuovo, non solo farlo funzionare.
  `load_dotenv()` senza path esplicito risale dal file CHIAMANTE, non dalla
  cwd corrente: uno script di test fuori dall'albero del repo non trova
  mai il vero `.env` — usare sempre un path esplicito nei test/script
  temporanei fuori dal repo.
- Prossimo passo: Alessandro riprova dal vivo domande via chat/dashboard e
  giudica se la velocita' percepita e' cambiata abbastanza. Se
  `JARVIS_CLI_BARE=1` risulta troppo costoso sul suo utilizzo reale
  (pagamento a consumo invece di abbonamento su questo canale), puo'
  rimetterlo a 0 in `.env` senza toccare codice.

## 17:30 — Battuta occhiali confermata dal vivo, browser-bug bloccato nel codice, daemon vocale avviato

- Fatto: Alessandro ha rifatto "jarvis apri la webcam e dimmi cosa vedi" dal
  canale web/dashboard — battuta occhiali corretta e reale ("potrebbe
  passare per napoletano"), ma il bug "apro la dashboard -> ERR_CONNECTION_
  REFUSED su localhost:3000" si e' ripresentato una terza volta (dopo 15/07
  mattina e sera) nonostante il SYSTEM prompt lo vieti gia' a parole. Non
  piu' un problema di wording: `core/browser.py::BrowserAgent.open()` ora
  rifiuta a livello di codice qualunque host locale (localhost/127.0.0.1/
  0.0.0.0/::1) PRIMA di toccare Playwright — se Claude emette comunque quel
  blocco, l'esito e' un messaggio pulito invece di un errore di rete.
  Ha anche chiesto perche' non sentiva piu' la voce nelle risposte.
  Verificato: quell'interazione era sul canale 'web' (channel nella riga
  Turso), che non ha MAI avuto sintesi vocale (solo riconoscimento vocale
  in ingresso via Web Speech API — verificato con grep su tutto app.js,
  zero `speechSynthesis`). L'unico componente che parla davvero e' il
  daemon vocale nativo (`core/voice/daemon.py`, hotkey Ctrl+Alt+J o "hey
  jarvis"), MAI avviato oggi (solo bot.py). Avviato ora
  (`.venv/Scripts/python.exe -m core.voice.daemon`), confermato vivo dopo
  18s (niente crash all'avvio/caricamento modelli). 45/45 test passano.
- Esito: ok. Bug browser bloccato strutturalmente (non solo a parole).
  Chiarito il malinteso voce: non era una regressione, il canale
  'web'/dashboard non ha mai parlato.
- Imparato: quando un'istruzione nel system prompt viene ignorata piu' di
  una volta nonostante sia scritta chiaramente, la soluzione e' un guard
  nel codice (verificabile, deterministico), non riscrivere ancora il
  prompt sperando funzioni questa volta.
- Prossimo passo: Alessandro puo' ora usare Ctrl+Alt+J per avere risposte
  vocali vere. Se vuole che ANCHE il canale dashboard "parli" (sintesi in
  browser via Web Speech API, senza passare dal daemon nativo), e' una
  feature separata da valutare — non ancora richiesta esplicitamente, non
  costruita.
