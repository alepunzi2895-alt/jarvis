## 09:53 — Promemoria calendario Outlook 15 min prima dei meeting

- Fatto: `core/outlook.py` legge il calendario Outlook via COM (olFolderCalendar,
  `IncludeRecurrences=True` + `Restrict` sulla finestra temporale — stesso
  pattern retry/thread-fresco gia' usato per la posta). Nuovo
  `bot.py::calendar_reminder_loop()` (poll ogni 60s, `JARVIS_CALENDAR_REMINDER_MINUTES`,
  default 15) avvisa su Telegram + voce (`speak_locally`) quando un evento
  entra nella finestra, con dedup in-memory (`_notified_events`, pulizia
  oraria). Aggiunto anche un intent on-demand ("che meeting ho oggi" /
  "agenda di oggi" ecc., `CALENDAR_INTENT_RE` in `core/outlook.py`,
  wired in `core/intents.py`). Branch `feature/outlook-calendar-reminders`,
  mergiato e pushato su main (commit `d0af659`).
- Esito: ok
- Imparato: la lettura calendario reale via COM funziona subito su questa
  macchina (Outlook desktop gia' autenticato, stesso presupposto della
  posta) — verificato dal vivo con `get_upcoming_events_sync()` (trovato
  l'evento reale "Daily Data Driven Brief" delle 10:05) e con l'intent
  completo via `parse_intent`/`execute_intent`. Nessun nuovo test unit per
  `_fetch_upcoming_sync` (tocca COM reale, stessa scelta gia' fatta per la
  posta) — 227/227 test verdi comunque (41 in `test_outlook.py`).
- Prossimo passo: non verificato dal vivo il loop `calendar_reminder_loop`
  end-to-end nel processo always-on (richiede il solito riavvio del bridge
  locale — bot.py va rilanciato per caricare il nuovo codice, vedi limite
  noto sui processi da Scheduled Task in `memory/projects/jarvis.md`).

## 09:59 — Conferma: bot.py vecchio ancora in esecuzione, serve riavvio manuale

- Fatto: Alessandro ha chiesto "che meeting ho oggi?" dalla dashboard e ha
  ricevuto la risposta del vecchio codice ("non ho integrazione calendario").
  Verificato: i 4 processi `pythonw.exe` dello Scheduled Task `\JARVIS\Bot`
  risultano avviati alle 09:42:30, PRIMA del merge (09:55 circa) — codice
  vecchio ancora in memoria. Tentato `Stop-ScheduledTask`/`Start-ScheduledTask`
  (riportano successo ma non toccano il processo reale) e `Stop-Process -Force`
  diretto sui PID: tutti falliti con "Accesso negato".
- Esito: fallito (limite noto, non una novita')
- Imparato: nessuna novita' rispetto a [[jarvis-cannot-kill-scheduled-tasks]]
  — riconfermato con lo stesso identico comportamento (successo apparente di
  Stop-ScheduledTask, Accesso negato su Stop-Process diretto). Serve sempre
  Alessandro in persona (Task Manager, ha accesso pieno) o un vero
  logoff/riavvio Windows.
- Prossimo passo: chiedere ad Alessandro di chiudere `pythonw.exe` (Task
  Manager) o riavviare il PC — dopo, `/progetti`, calendario, e tutte le
  feature mergiate oggi risultano attive senza altro intervento (autostart
  gia' configurato).

## 10:15 — Rimosse le pill workspace dalla dashboard, brain unificato

- Fatto: Alessandro non voleva piu' vedere le pill progetto (jarvis/aura/
  whitesoul/trading/isabela/vino) in cima alla dashboard ne' il grafo
  second brain diviso/attenuato per workspace attivo. Chiesto prima come
  gestire il routing (le pill sceglievano anche la cartella/repo su cui
  JARVIS opera) — risposta: auto-detect dal testo. Implementato
  `core/claude_bridge.py::detect_workspace()` (parole chiave per
  progetto, default "jarvis"), usato in `core/web_bridge.py` per la
  dashboard (il bot Telegram resta sul comando esplicito `/ws`, non
  toccato). Rimossa la barra pill (`web/public/index.html`/`app.js`/
  `style.css`), il client non sceglie piu' il workspace (niente piu'
  `localStorage["jarvis_ws"]`). Grafo second brain: un solo colore per
  tutti i nodi (`BRAIN_NODE_COLOR`, accent fisso #3987e5) invece della
  palette per-workspace con dimming dei nodi fuori dal workspace attivo —
  quel dimming era probabilmente la causa principale del fastidio
  segnalato ("contesti" visivamente separati). L'HUD "Progetto" ora è
  di sola lettura (mostra l'ultimo rilevato, non piu' selezionabile).
- Esito: ok
- Imparato: le pill non erano solo estetiche — sparire senza sostituto
  avrebbe rotto il routing multi-repo (read/write/run_command nella
  cartella sbagliata). L'auto-detect a parole chiave e' un compromesso
  semplice: non serve NLU, il default "jarvis" copre tutto il resto.
  237/237 test verdi (10 nuovi: `test_claude_bridge.py::test_detect_workspace_*`,
  `test_web_bridge.py::test_intent_result_is_tagged_with_auto_detected_workspace`).
- Prossimo passo: non verificato dal vivo nel browser reale (serve login
  dashboard, password non presente in questo ambiente) — verificare dopo
  il deploy che l'header non abbia buchi di layout e che un messaggio
  tipo "controlla aura" venga instradato davvero sul progetto giusto.

## 11:53 — "Facciamo tutti e 5 i punti": briefing, presenza, bozze, trading, Iveco

- Fatto: dopo la domanda "cosa possiamo implementare ancora da vero
  Jarvis" (risposta con 5 proposte) e la conferma "facciamo tutti e 5",
  chiesto prima cosa serviva davvero decidere lui (fonte dati per la
  dashboard finanziaria, scope del MCP router) — risposte: "solo trading
  per ora" (confermato da un fork di ricerca: solo Myfxbook/TradeFlow ha
  una fonte dati realmente strutturata, AURA/WhiteSoul/AP Systems no) e
  "solo routing Iveco" (niente altri MCP server). Costruiti tutti e 5 i
  blocchi in una sessione, branch `feature/jarvis-five-features`:
  1. **Briefing mattutino** (`core/briefing.py`, nuovo): meteo + calendario
     di oggi + mail non lette + trading (se configurato) + stato progetti
     in un'unica risposta. `/buongiorno`/`/briefing` su Telegram, intent
     "buongiorno" (ancorato a inizio/fine frase per non rubare un comando
     composto tipo "buongiorno apri chrome"), sostituisce il vecchio
     digest mattutino (`cmd_progetti` rimossa, dead code).
  2. **Presenza proattiva** (`core/presence.py`, nuovo): webcam +
     riconoscimento volto gia' esistenti, ora anche in un loop periodico
     (`JARVIS_PRESENCE_INTERVAL_SEC`, default 0 = disattivo — va acceso di
     proposito) che saluta da solo alla transizione assente->presente
     (streak anti-flicker + cooldown 30 min, mai un saluto per frame
     perso). `core/voice/camera.py` esposto `capture_frame_raw()`.
  3. **Bozze generalizzate**: `core/outlook.py::create_draft_email()`
     (nuovo, unica eccezione a "sola lettura" — SOLO `.Save()`, mai
     `.Send()`, verificato dal vivo: creata una bozza vera di test in
     Outlook, controllata via COM che fosse davvero in Bozze, poi
     eliminata) + `core/mail_actions.py` (blocco ```mail_draft```, stesso
     pattern di browser/system/databricks). Per Teams: `core/browser.py`
     aveva GIA' `type_text(submit=False)` — aggiunta solo una barriera **a
     livello di codice** (non solo prompt, stesso principio del `git push`
     sempre-confermato) che forza `submit=False` su teams.microsoft.com/
     outlook.office.com a prescindere da cosa chiede Claude. SYSTEM prompt
     aggiornato per usare entrambi invece del vecchio "scrivi solo la
     bozza a parole, l'utente la copia".
  4. **Dashboard finanziaria trading** (`core/myfxbook.py`, nuovo): API
     ufficiale Myfxbook (login/get-my-accounts/logout ad ogni chiamata,
     nessuna sessione persistita — uso sporadico), stesso schema gia'
     usato da `tradeflow-ai/api/myfxbook.js` (letto per capire il formato
     risposta/gestione errori). Richiede `MYFXBOOK_EMAIL`/
     `MYFXBOOK_PASSWORD` (non ancora impostate in questo ambiente — la
     sezione degrada a "non configurato" ovunque, briefing incluso).
     Intent "come va il trading", loop `trading_snapshot_loop` (push
     Turso ogni 15min) + pannello dashboard nascosto (`#trading-cards`)
     finche' non arriva una riga vera.
  5. **EnvironmentRouter, routing Iveco**: aggiunta la voce "iveco" a
     `WORKSPACES`/`_WORKSPACE_KEYWORDS` (databricks/qlik/unity catalog/
     pyspark/genie/dbsql) — Genie/dbSQL restavano gia' disponibili sempre,
     qui serve solo per read_file/write_file/run_command e il second
     brain quando si parla esplicitamente di lavoro Iveco.
- Esito: ok
- Imparato: `.project-cards[hidden]` non bastava da solo a nascondere il
  pannello trading — la classe imposta gia' `display:flex`, che vince
  sulla regola UA di `[hidden]` a prescindere dalla specificity (stesso
  bug pattern gia' risolto una volta per `.camera-ask-form[hidden]`,
  non ricordato finche' non l'ho ritrovato in style.css — vale come
  promemoria per qualunque nuovo pannello nascosto in futuro).
- Prossimo passo: **non verificato dal vivo**: presenza proattiva (serve
  lui fisicamente + `/enroll_face` gia' fatto), bozza Teams via browser
  (Teams non e' mai stato loggato nel browser persistente di JARVIS —
  limite gia' noto, non nuovo), pannello trading dashboard (richiede
  `MYFXBOOK_EMAIL`/`MYFXBOOK_PASSWORD` che lui deve aggiungere), routing
  "iveco" con un vero `WS_IVECO` configurato. Verificato dal vivo invece:
  bozza Outlook reale, calendario/meteo/mail nel briefing con dati veri,
  auto-detect "iveco" dal testo. 271/271 test verdi (44 nuovi).

## 12:20 — Segnalato "non mi riconosce più quando parlo": VoiceDaemon fermo

- Fatto: Alessandro segnala che il riconoscimento vocale non funziona
  piu'. Controllato `\JARVIS\VoiceDaemon` (Scheduled Task): stato
  "Ready" (NON in esecuzione), `LastRunTime` 09:42:42 — nessun processo
  `pythonw.exe` corrispondente in memoria, a differenza di `\JARVIS\Bot`
  (Running, riavviato pulito alle 12:14:44). Causa quasi certa: quando
  gli ho chiesto di chiudere `pythonw.exe` da Task Manager per far
  ripartire il Bot con il codice nuovo (dopo il merge calendario delle
  09:55), ha chiuso TUTTI i processi `pythonw.exe` — Bot e VoiceDaemon
  girano entrambi con quel nome, quindi ha ucciso anche il daemon vocale
  senza saperlo. Non e' un bug introdotto dalle modifiche di oggi: il
  daemon non ha semplicemente attività di log dopo l'avvio (comportamento
  normale, si vede solo l'ultima riga "attivo" finche' non arriva un
  comando vero).
- Esito: ok — `Start-ScheduledTask` su un task GIA' FERMO ha funzionato
  senza il solito "Accesso negato" (quel limite riguarda solo il TERMINARE
  un processo gia' in esecuzione lanciato da uno Scheduled Task, non
  l'AVVIARE un task che risulta gia' fermo — distinzione utile per il
  futuro, vedi [[jarvis-cannot-kill-scheduled-tasks]]). Nuovo processo
  partito alle 12:19:44, log pulito (whisper CPU fallback noto, wake
  word caricato, "daemon vocale attivo"), verificato ancora vivo dopo
  ~10s (non ricrashato subito).
- Imparato: "chiudi pythonw.exe" come istruzione e' ambigua quando piu'
  processi JARVIS condividono lo stesso eseguibile (Bot + VoiceDaemon) —
  in futuro specificare quale PID/finestra chiudere, o controllare
  entrambi i task dopo un riavvio manuale invece di darlo per scontato.
- Prossimo passo: **non verificato dal vivo** che il riconoscimento vocale
  risponda davvero ("hey jarvis"/Ctrl+Alt+J) — richiede lui a microfono
  acceso. Se si ripresenta, controllare SEMPRE entrambi i task JARVIS
  dopo qualunque "chiudi pythonw.exe", non solo quello che si voleva
  toccare.

## 12:47 — JARVIS puo' usare VS Code + delegare modifiche vere a Claude Code

- Fatto: richiesta esplicita di Alessandro ("rendere Jarvis in grado di
  usare VS Code, aprire progetti, scrivere prompt al Claude Code per
  modifiche"). Chiesto prima ambito (solo progetti gia' noti/autorizzati
  vs path arbitrari) e livello di autonomia (modifiche libere vs
  approvazione preventiva) — risposte: entrambe le opzioni consigliate
  ("solo progetti noti" + "autonomo come oggi"). Costruito:
  `core/system_executor.py::open_vscode()` (nuovo — `code <path>`, whitelist
  obbligatoria, nessun token di conferma perche' chi chiama decide gia'
  quali progetti sono autorizzati prima di arrivare qui); `core/
  vscode_actions.py` (nuovo — blocco ```vscode``` {"project","prompt"},
  stesso pattern di browser/mail_actions; "project" solo tra le chiavi di
  `WORKSPACES` — jarvis/aura/whitesoul/trading/isabela/vino/iveco;
  "prompt" facoltativo, se assente apre solo VS Code). Se c'e' un prompt,
  lancia una VERA sessione `claude -p --permission-mode acceptEdits` in
  un thread di sfondo (mai `asyncio.create_task`: lo stesso problema gia'
  documentato in `brain.log_interaction`, un loop che chiude a fine
  chiamata — voce — cancellerebbe il task prima del termine) e notifica
  l'esito su Telegram quando finisce, mai nella risposta immediata (un
  task di coding vero puo' metterci minuti). Aggiunto anche `/code
  <progetto> <prompt>` su Telegram per un uso diretto senza passare da
  Claude. SYSTEM prompt aggiornato con l'elenco progetti autorizzati.
- Esito: ok
- Imparato: import circolare reale scoperto e risolto — `core/
  vscode_actions.py` aveva bisogno di `WORKSPACES`/`CLAUDE_BIN`/
  `MAX_TURNS` da `core/claude_bridge.py`, che a sua volta importa
  `vscode_actions` per il blocco (stesso schema di browser/mail_actions).
  Fix: `CLAUDE_BIN`/`MAX_TURNS` duplicati come semplici letture di env var
  (nessun accoppiamento reale), `WORKSPACES` importato SOLO dentro la
  funzione che lo usa (deferred import, il ciclo esiste solo a livello di
  modulo). Verificato dal vivo: aperto VS Code sul repo `jarvis` stesso
  (`open_vscode` reale, nessun errore) e lanciata una VERA sessione
  Claude Code CLI con un prompt vincolato ("non modificare nulla, rispondi
  solo TEST OK") — `git status` invariato prima/dopo, messaggio Telegram
  formattato correttamente ("✅ ... TEST OK").
- Prossimo passo: **non verificato dal vivo** un vero task di modifica
  codice (ho testato solo un prompt a vuoto per non rischiare scritture
  indesiderate durante il test) — la prima volta che lo usa per davvero
  vale la pena guardare l'esito con attenzione. 284/284 test verdi (13
  nuovi in `test_vscode_actions.py`).

## 13:06 — Bug reale: "si e' bloccato" su "leggimi le mail" — trovata e risolta causa

- Fatto: Alessandro segnala che "Hi Jarvis, puoi aprirmi le mail e
  leggermi l'ultima" (dashboard, mic a mani libere) si e' bloccato, e
  in generale vuole piu' velocita'/efficienza nel dialogo. Controllato
  `logs/bot.log`: trascrizione e wake word OK, ma subito dopo compare
  "controllo calendario fallito (ignorato): ... Esecuzione del server
  non riuscito (0x80080005)" — lo stesso errore che l'utente ha rivisto
  poco dopo chiedendo aiuto direttamente. Causa reale: `calendar_
  reminder_loop` (nuovo di oggi, interroga Outlook via COM ogni 60s in
  background) e la sua domanda vocale sulla posta sono capitati nello
  stesso istante — due thread diversi che aprono ciascuno un proprio
  `Dispatch("Outlook.Application")` in parallelo si scontrano DENTRO
  Outlook.exe (che processa le chiamate COM in arrivo essenzialmente una
  alla volta), non e' un problema di configurazione ne' un blip isolato
  come pensato inizialmente (vedi commento del 2026-09-15 in
  `core/outlook.py`) — i retry da soli non bastavano perche' l'altra
  chiamata poteva essere ancora in corso ad ogni nuovo tentativo.
- Fix: `core/outlook.py::_COM_LOCK` (nuovo, `threading.Lock()`) dentro
  `_run_in_fresh_thread` — serializza OGNI accesso Outlook (mail,
  calendario, bozze) all'interno del processo, cosi' due chiamate non
  sono mai davvero concorrenti, si mettono in coda invece di scontrarsi.
  `bot.py::calendar_reminder_loop` da 60s a 120s tra un controllo e
  l'altro (meno traffico COM in background, nessuna perdita pratica
  sulla finestra di preavviso di 15 minuti). Per la richiesta generale
  di velocita': accorciato il SYSTEM prompt di `core/claude_bridge.py`
  (i tre paragrafi aggiunti oggi per mail_draft/Teams/vscode erano
  verbosi) — da 8859 a 7842 caratteri (-11%), iniettato in OGNI turno,
  voce inclusa (Haiku).
- Esito: ok — verificato dal vivo lanciando mail+calendario davvero in
  parallelo (due thread, stesso istante): prima del fix questo era
  esattamente lo scenario che falliva, dopo il fix entrambi vanno a
  buon fine in 0.25s totali, zero errori.
- Imparato: un blip Outlook COM "raro" puo' diventare frequente/sistemico
  nel momento in cui si aggiunge un loop periodico che tocca la STESSA
  risorsa esterna — vale come promemoria generale per qualunque futuro
  loop background che chiama API esterne/COM: verificare se esiste gia'
  un altro punto del codice che chiama la stessa cosa, e se si' serializzare
  esplicitamente invece di fidarsi dei soli retry.
- Prossimo passo: nessuno bloccante. 285/285 test verdi (1 nuovo,
  concorrenza Outlook). Se la lentezza generale persiste dopo questo fix,
  il prossimo passo naturale sarebbe un SYSTEM prompt diverso/piu' corto
  per il solo canale vocale (oggi condivide lo stesso testo completo del
  canale testo, gran parte irrilevante per una chiacchierata vocale
  casuale) — non fatto oggi, cambio piu' invasivo.

## Pomeriggio — 4 segnalazioni: VS Code "errore 18", Outlook nel browser, "stop" vocale, calendario "domani"

- Fatto: Alessandro segnala in sequenza (dashboard, uso reale non un test):
  (1) aprire VS Code da JARVIS da' "errore launch codice 18"; (2) leggere
  Outlook nel browser deve filtrare i bottoni e leggere solo le mail
  aprendole, con riassunto; (3) dire "Jarvis, stop" deve fermare subito la
  lettura; (4) "che riunioni ho domani" risponde "non posso collegarmi al
  calendario" nonostante il calendario fosse gia' stato costruito stamattina
  (vedi 09:53). Analizzato `logs/bot.log` reale (non un test): conferma
  tutti e 4 i problemi dal vivo.
- Causa (4) calendario: `CALENDAR_INTENT_RE` richiedeva "ho"/"ci sono"
  letterali — la STT aveva trascritto "che riunioni ho in programma domani"
  come "che riunioni un programma domani" (il verbo "mangiato"), nessuna
  alternativa del regex incrociava, la domanda cadeva intera su Claude
  (motore API diretto, v5) che NON ha alcun accesso al calendario (solo
  `core/intents.py` lo interroga via COM) — da qui la risposta onesta ma
  sbagliata "non posso collegarmi". Inoltre anche quando l'intent matchava,
  la finestra era sempre "prossime 12 ore da ora", mai il giorno di domani
  per intero.
- Causa (2) Outlook nel browser: confermato nel log — `browser.read()`
  faceva `inner_text("body")` su Outlook Web, tornando ribbon/albero
  cartelle/anteprime elenco messaggi E il corpo della mail aperta tutti
  concatenati. JARVIS lo leggeva poi ad alta voce parola per parola (righe
  27-85 del log: "Crea nuovo messaggio", "Sposta in cartella di
  archiviazione", nomi di cartelle — captate persino dal microfono come
  falso nuovo comando, incastrate nella trascrizione stessa, anche se
  scartate perche' senza wake word).
- Causa (3) "stop": nessun intent riconosceva la parola da sola — il
  barge-in (`tts.stop_current_speech()` in `web_bridge.py`) interrompeva
  gia' l'audio su QUALUNQUE nuovo comando vocale, "stop" compreso, ma poi
  "stop" veniva comunque passato a Claude come richiesta vera, che generava
  una risposta nuova e JARVIS ricominciava a parlare subito — l'opposto di
  quanto chiesto.
- Causa (1) VS Code: **non trovata con certezza**. `core/system_executor.py::
  open_vscode()` risolve `Code.exe` via registro Windows (verificato dal
  vivo: App Paths ha davvero la voce, punta a
  `AppData\Local\Programs\Microsoft VS Code\Code.exe`, file esistente) e il
  log del vault (`jarvis/Logs/system-2026-09-16.md`) conferma DUE
  `open_vscode` odierni entrambi loggati come riusciti (nessuna eccezione
  Python catturata) — quindi il fallimento non e' nel `Popen` stesso, deve
  essere un errore che VS Code (o Windows/EDR) mostra DOPO l'avvio, non
  intercettabile da qui senza sapere il testo esatto. **Bloccante: chiesto
  ad Alessandro il testo/screenshot esatto dell'errore**, non ancora
  risposto in questa sessione.
- Fix applicati (branch `fix/calendar-stop-outlook-browser`, mergiato e
  pushato su main, commit `a7c21f4`/`e25226a`):
  - `core/outlook.py`: `CALENDAR_INTENT_RE` piu' permissivo (tollera "ho"
    mangiato dalla STT, nuove alternative senza verbo coniugato
    obbligatorio); nuova `get_events_for_day_sync(target)` — finestra sul
    giorno SOLARE (00:00-23:59) invece che relativa a "ora", usata quando
    il testo contiene "domani"; `format_events(..., day=...)` per la frase
    di risposta corretta.
  - `core/intents.py`: nuovo intent `"stop"` (stop/basta/fermati/smettila/
    taci/zitto/silenzio, ancorato a frase intera) che chiama
    `tts.stop_current_speech()` e ritorna senza mai passare da Claude;
    `bot.py`/`core/web_bridge.py` non pronunciano la sua conferma (unico
    intent per cui il TTS della risposta e' saltato apposta).
  - `core/browser.py`: nuova `_read_scoped()` — legge SOLO il riquadro di
    lettura (role ARIA `"document"`, standard e indipendente dalla lingua
    dell'interfaccia) se una mail e' aperta, altrimenti SOLO l'elenco
    messaggi (role `"option"`) — mai il body intero su Outlook Web. Nuova
    azione ```browser``` `{"action":"read_mail","count":N}`: apre in
    sequenza le prime N righe dell'elenco e ritorna solo il corpo di
    ciascuna, Claude le riassume nella risposta (il codice non riassume da
    solo). SYSTEM prompt (`core/claude_bridge.py`, condiviso da CLI e
    motore API diretto) aggiornato per dirlo a Claude esplicitamente.
- Esito: parziale — 3 problemi su 4 corretti e testati (297/297 test
  verdi, 12 nuovi), il quarto (VS Code) resta aperto in attesa di
  informazioni da Alessandro.
- Imparato: quando un intent a regex fallisce silenziosamente per una
  frase STT imperfetta, la richiesta non "fallisce" in modo visibile — cade
  su un motore (Claude/API diretta) che magari non ha ALCUN accesso allo
  strumento richiesto, e risponde in modo plausibile ma sbagliato ("non
  posso collegarmi"). Vale come promemoria per qualunque futuro intent a
  regex stretto: se lo strumento sottostante non e' raggiungibile in
  nessun altro modo da Claude, un miss del regex e' silenzioso e
  fuorviante, non innocuo.
- Prossimo passo: chiedere il testo/screenshot esatto dell'errore VS Code
  ad Alessandro (chiesto, in attesa). Riavvio del bridge locale necessario
  perche' i 3 fix passino dal codice al comportamento reale (bot.py/
  VoiceDaemon gia' in esecuzione dalle 13:10, precedono questo merge).
  **Non verificato dal vivo**: la lettura mirata Outlook Web (nessuna
  sessione Outlook Web reale raggiungibile da questa sessione per
  controllare i selettori ARIA contro la UI vera — i test coprono solo la
  logica di dispatch con mock).

## Ricerca su richiesta: Mark LIII (github.com/FatihMakes/Mark-LIII) per spunti

- Fatto: Alessandro ha chiesto di guardare questo progetto (assistente
  vocale cross-platform, Gemini Live API, PyQt6) per idee da riprendere.
  Letto il README via WebFetch (nessuna clonazione, solo lettura).
  Punti distintivi trovati: (1) memoria "senza limiti" — solo core+fatti
  recenti nel prompt, il resto cercato on-demand via `recall_memory`
  invece di iniettare tutto sempre; (2) undo reversibile per azioni
  distruttive (spostamenti/rinomina/scritture file, limiti dichiarati: no
  file >1MB, no cancellazione di copie); (3) conferme irreversibili
  (shutdown/riavvio/WiFi) con token emesso dall'INTERFACCIA, non dal
  modello — JARVIS lo fa gia' cosi' (`SystemExecutor._stage_confirmation`,
  uuid server-side, non inventato da Claude), nessun gap qui; (4) wake
  word locale offline con auto-sleep dopo 2 minuti di silenzio; (5) plugin
  system "un file = una skill" — JARVIS ci somiglia gia' (browser.py/
  mail_actions.py/vscode_actions.py, stesso pattern blocco+extract_and_execute).
- Esito: solo ricerca, nessun codice toccato.
- Imparato: il punto (4) e' l'unico che tocca un limite REALE gia'
  documentato di JARVIS (il modello wake word "hey_jarvis" non riconosce
  la voce/accento di Alessandro, punteggio sempre <0.02 — vedi log
  2026-07-15) — Mark LIII non risolverebbe questo da solo (il loro wake
  word e' comunque un modello generico, non allenato sulla sua voce); il
  problema di JARVIS non e' "wake word online vs offline" (gia' offline),
  e' che il modello specifico non riconosce lui. (1) e (2) sono le idee
  piu' concretamente utili non ancora presenti in JARVIS.
- Prossimo passo: chiesto ad Alessandro se vuole che costruisca (1) memoria
  on-demand o (2) undo reversibile — risposto (1). Costruita subito dopo,
  vedi sezione successiva. (2) undo reversibile resta un'idea non
  implementata, da riprendere se torna utile.

## Memoria on-demand nel second brain (branch feature/brain-recall-memory, mergiato+pushato, commit acc6bae)

- Fatto: su richiesta di Alessandro (vedi sopra), costruito
  `core/brain.py::search(query)` — cerca su Turso (LIKE su label/summary/
  tags, esclude i nodi "interazione" come fetch_context) oltre ai 15 nodi
  gia' iniettati automaticamente in ogni turno. Esposto come tool nativo
  Anthropic `recall_memory` in `core/claude_bridge.py::TOOLS`/
  `_execute_tool` — stesso motore (API diretta) usato di default da
  testo/dashboard/Telegram dal v5 di oggi mattina, quindi disponibile
  ovunque tranne la voce nativa (motore separato, nessun tool).
- Esito: ok, 305/305 test verdi (9 nuovi: `test_brain.py::test_search_*`,
  `test_claude_bridge.py::test_execute_tool_recall_memory_*`).
- Imparato: non serve ridurre l'iniezione automatica (gia' capped a 15
  nodi via `CONTEXT_LIMIT`, non "tutto" come nel problema che Mark LIII
  risolveva) — il vero gap era la mancanza di un modo per Claude di
  cercare OLTRE quei 15 quando servono. Aggiunto solo quello, cambio
  minimo e mirato.
- Prossimo passo: **non verificato dal vivo** (nessuna query reale di
  Claude che ha davvero usato `recall_memory` in questa sessione — il
  tool esiste ma non e' mai stato esercitato contro un second brain con
  dati veri). Da controllare la prima volta che un turno reale lo
  invoca.

## Meteo di "domani" e bug reale Teams/schermo (immagine mai funzionata)

- Fatto: due segnalazioni live in sequenza.
  1. "Jarvis, il tempo farà domani" -> risposta onesta ma sbagliata ("non
     ho accesso diretto alle previsioni", apre una ricerca Google). Causa:
     `_WEATHER_RE` matchava solo "che tempo fa" (presente, oggi);
     `get_weather_line()` inietta nel system prompt SOLO il meteo di oggi
     (ambient, come l'ora) — Claude non ha mai avuto ne' l'intent ne' un
     dato per "domani", nonostante `core/weather.py::get_weekly_forecast()`
     esista gia' da tempo (usato pero' solo dal pannello dashboard, mai
     raggiungibile da una domanda). Fix: regex piu' permissivo (tollera
     "farà"/"meteo"/"previsioni"), nuova `weather.format_tomorrow_line()`
     che riusa `get_weekly_forecast()` e prende `days[1]`.
  2. "Jarvis, fammi vedere chi mi ha scritto su Teams" -> **errore API
     reale** (400, non un rifiuto di Claude): `messages.16.content.0.image
     .source.base64: ... specificata come image/jpeg ma sembra un'immagine
     image/png`. Causa: `core/claude_bridge.py`/`core/claude_api.py`
     dichiaravano SEMPRE `"media_type":"image/jpeg"` quando allegavano
     un'immagine a Claude — corretto per la webcam (`core/voice/camera.py`
     usa `cv2.imencode(".jpg", ...)`) ma FALSO per gli screenshot di
     Teams/schermo (`core/screen_context.py`, PNG veri via
     `page.screenshot()`/GDI+). **Conseguenza reale**: leggere Teams o lo
     schermo non ha MAI funzionato dal 2026-09-14 (data di introduzione di
     `screen_context.py`) — ogni tentativo falliva con questo 400, mai
     notato prima perche' testato solo con la webcam (sempre JPEG,
     nessun problema). Fix: `_detect_image_media_type()` (sniffing sui
     byte magici PNG `\x89PNG\r\n\x1a\n`, altrimenti JPEG) al posto del
     valore fisso, in entrambi i motori.
  Chiesto anche di filtrare l'interfaccia su Teams (vedere solo chat+
  messaggi, non bottoni/rail). Aggiunto `BrowserAgent.screenshot(scoped=
  True)`: prova a fotografare solo la regione ARIA "main" della pagina
  invece dell'intera finestra (esclude rail/nav se fuori da `<main>`),
  usato da `screen_context._capture_browser_page` per Teams. SYSTEM prompt
  istruito esplicitamente a riportare solo chi scrive + testo dei
  messaggi, non descrivere la UI.
- Esito: ok, 316/316 test verdi (19 nuovi). Il bug (2) e' il piu' serio
  trovato oggi: non un edge case, una feature intera (leggere Teams/
  schermo) rotta al 100% da settimane senza che nessun log lo segnalasse
  in modo ovvio (l'errore arrivava dall'API Anthropic, non da JARVIS).
- Imparato: "testato solo col caso d'uso originale (webcam)" per un
  parametro hardcoded che sembra ovvio (media_type di un'immagine) e'
  esattamente il tipo di assunzione che si rompe silenziosamente quando
  lo stesso codice viene riusato per una sorgente diversa (screenshot) —
  vale come promemoria: quando una funzione condivisa acquisisce una
  seconda sorgente di dati, ricontrollare ogni valore che sembrava
  "costante" nella prima versione.
- Prossimo passo: **non verificato dal vivo** — richiede che Alessandro
  riprovi "leggi Teams"/"guarda lo schermo" dopo il riavvio del bridge
  (sia il fix del 400 sia il crop "scoped" sono nuovi, mai visti contro
  una vera sessione Teams). Se il login Teams nel browser di JARVIS non è
  mai stato fatto, la cattura mostrerà la pagina di login (comportamento
  atteso, non un nuovo bug).

## Meteo: mai piu' una ricerca browser, sempre il tool con dati veri

- Fatto: Alessandro segnala che "che tempo fa" a volte fa comunque aprire
  una ricerca browser invece di usare il "widget meteo che ho gia'"
  (il pannello previsioni della dashboard, alimentato da
  `weather.get_weekly_forecast()`). Causa: il fast-path di
  `core/intents.py` copre solo "oggi"/"domani" — qualunque altro fraseggio
  (un giorno della settimana, "tra 3 giorni", ecc.) cade su Claude, che
  fino ad ora non aveva NESSUN modo di consultare la previsione se non
  aprendo un browser (stesso schema del bug calendario di oggi
  pomeriggio: intent stretto + nessuna alternativa per Claude = ripiego
  peggiore).
- Fix: `weather.format_day_line(days_ahead)` (0-6, riusa
  `get_weekly_forecast()` — la stessa fonte dati del widget dashboard,
  `format_tomorrow_line()` ora e' solo un alias di `format_day_line(1)`)
  esposto come tool nativo `get_weather_forecast` in
  `core/claude_bridge.py::TOOLS` — stesso pattern di `recall_memory` di
  oggi pomeriggio. SYSTEM prompt aggiornato: l'azione di ricerca browser
  ora dice esplicitamente "MAI per il meteo, usa get_weather_forecast".
- Esito: ok, 320/320 test verdi (7 nuovi).
- Imparato: quando un fast-path a regex copre solo un sottoinsieme dei
  casi (qui "oggi"/"domani" ma non gli altri giorni), il vero fix
  strutturale e' dare a Claude un tool con GLI STESSI dati reali per il
  resto dei casi, non provare a rincorrere ogni fraseggio possibile con
  regex sempre piu' complesse — pattern gia' visto oggi con calendario/
  second brain, terza volta nello stesso pomeriggio.
- Prossimo passo: **non verificato dal vivo** (nessuna chiamata reale di
  Claude che ha invocato `get_weather_forecast` in questa sessione).

## Flusso interattivo mail/Teams + stato progetti senza percentuali, su domanda

- Fatto: due richieste in sequenza.
  1. Mail/Teams: Alessandro vuole che leggere la posta/le chat non sia piu'
     "elenca tutto e basta" ma un vero botta-risposta: prima chiede se
     vuole solo le non lette o tutte, poi elenca per oggetto (mail) o nome
     (Teams) e chiede quale aprire, poi apre SOLO quella e ne fa un
     riassunto breve. Nel mezzo, ha anche incollato un errore reale
     capitato provando a "vedere chi ha scritto su Teams" (vedi sezione
     precedente per il fix del 400 immagine).
  2. Progetti: togliere le percentuali di "salute" (sintetiche, mai
     richieste dopo l'integrazione GitHub/Vercel reale — `core/
     github_status.py`/`core/vercel_status.py`, "tanto teniamo traccia di
     vercel e github") e, quando chiede lo stato genericamente, farsi
     chiedere PRIMA quale progetto invece di ricevere il report di tutti
     e 3 non richiesto.
- Fix (1): `core/outlook.py` — nuovo `EmailDetail` (corpo COMPLETO, non lo
  snippet a 200 caratteri di `EmailSummary`), `find_email_sync(query,
  unread_only)` (cerca per mittente/oggetto, sottostringa case-insensitive),
  `format_email_list_for_picking()`/`format_email_detail()`. Esposti come
  due tool nativi Claude in `core/claude_bridge.py`: `list_emails`
  (elenco, niente contenuto) e `read_email` (corpo completo di UNA mail).
  **Rimosso il fast-path** di `core/intents.py` per "leggi le mail"/
  "controlla la posta" (restava solo per il conteggio "quante mail ho") —
  senza questo, quelle frasi avrebbero continuato a rispondere subito con
  tutto l'elenco, saltando il flusso a domande che ora vive nel SYSTEM
  prompt. Per Teams (nessuna API strutturata, solo browser): SYSTEM prompt
  istruito a fare le stesse domande e poi usare i primitivi ```browser```
  read/click gia' esistenti sulla pagina persistente (che resta aperta tra
  un turno e l'altro, a differenza dello screenshot-vision che serve solo
  al turno in cui viene catturato).
- Fix (2): rimosso interamente `health_percent`/`_compute_health_percent`/
  `last_commit_ts` da `core/project_status.py` (formula a punteggio mai
  richiesta di nuovo dopo l'integrazione GitHub/Vercel, "un numero in piu'
  ridondante"), e la relativa barra/percentuale dalla dashboard (`web/
  public/app.js`, `.health-bar`/`.health-fill` morte rimosse da
  `style.css`). Nuovo `check_one_by_key(key, executor)` (un solo progetto,
  non tutti e 3) esposto come tool `get_project_status`. **Rimosso il
  fast-path** `_PROJECT_STATUS_RE` di `core/intents.py` (solo la frase
  generica "stato progetti"/"come vanno i progetti", MAI quando nomina gia'
  un progetto specifico — quella frase non passava comunque da qui) —
  Claude ora chiede quale prima di chiamare il tool, SYSTEM prompt
  aggiornato.
- Esito: ok, 325/325 test verdi (16 nuovi). Terza volta oggi che lo stesso
  fix strutturale si ripete: un fast-path a regex troppo rigido/eager
  viene ridotto o rimosso, e la richiesta cade su Claude con un tool vero
  al posto di una risposta immediata — pattern ormai consolidato per
  qualunque futura richiesta di comportamento "prima chiedi, poi agisci".
- Imparato: rimuovere un fast-path e' un cambio di comportamento non solo
  di velocita' — "leggi le mail"/"stato progetti" ora passano SEMPRE da
  un giro Claude reale (costo/latenza non piu' zero), scelta consapevole
  perche' l'utente ha chiesto esplicitamente l'interattivita', non una
  regressione accidentale.
- Prossimo passo: **non verificato dal vivo** (nessuna sessione reale che
  ha attraversato il flusso completo "non lette o tutte -> elenco ->
  quale -> riassunto" per mail o Teams, ne' una vera "stato progetti ->
  quale progetto -> report"). Limite noto e non risolto oggi: il flusso
  Teams multi-turno dipende da Claude che sceglie di riusare
  ```browser```/read invece di ri-scattare uno screenshot — non testato
  se lo fa davvero in modo affidabile.

## Voce multilingue non voluta + "ogni tanto si incarta" (segnalato, in indagine)

- Fatto: Alessandro segnala due cose insieme: "ogni tanto si incarta" (vago,
  chiesto un esempio concreto, non ancora arrivato) e "perche' mi parla
  anche in altre lingue? vorrei solo italiano" — questo secondo gia'
  risolvibile con certezza.
- Causa (lingua): `core/voice/tts.py::VOICE` era
  `it-IT-GiuseppeMultilingualNeural` — le voci "Multilingual" di edge-tts/
  Azure riconoscono le SINGOLE parole non italiane (GitHub, Vercel, VS
  Code, Teams... ricorrono in ogni risposta tecnica) e cambiano
  pronuncia/accento SOLO su quelle, a meta' frase. Il fix del 2026-09-15
  (ripulire il testo verboso per la voce) non bastava perche' il problema
  non era il testo ma la voce stessa che alterna lingua parola per parola.
- Fix: default cambiato a `it-IT-DiegoNeural` (voce italiana NON
  multilingua — pronuncia tutto con fonetica italiana, comprese le parole
  inglesi, invece di alternare). Aggiornato anche `.env.example`.
  Verificato che il `.env` reale di questa macchina non lo sovrascriveva
  (nessun `JARVIS_TTS_VOICE` impostato) — il nuovo default si applica
  davvero. 325/325 test verdi (nessun test dipendeva dal valore).
- "Si incarta": **non ancora diagnosticato con certezza**, chiesto un
  esempio concreto (cosa ha detto, cosa ha risposto JARVIS). Ipotesi
  investigata via codice, non confermata: `core/claude_bridge.py::
  _run_claude_api()` tiene la history della conversazione per-workspace
  (`state["api_sessions"][ws]`), e `ws` viene ri-rilevato da
  `detect_workspace()` ad OGNI messaggio dal solo testo (nessuna parola
  chiave -> default "jarvis"). Se il primo messaggio di un flusso a piu'
  passaggi (es. mail/Teams di oggi) nomina un progetto specifico ma la
  risposta breve successiva ("tutte"/"quella di Mario") no, i due turni
  finiscono in due "cassetti" di history diversi e Claude perde il filo
  di cosa aveva chiesto — plausibile ma NON verificato dal vivo, non
  corretto oggi in attesa di un esempio reale che lo confermi (rischio di
  costruire un fix per il problema sbagliato altrimenti).
- Prossimo passo: aspettare un esempio concreto di "si incarta" da
  Alessandro prima di toccare altro codice.

## "Si incarta" chiarito: risponde in ordine a domande vecchie superate

- Fatto: Alessandro precisa "si incarta" -> "ogni tanto prosegue a
  rispondere con vecchie domande" e chiede esplicitamente: "ad ogni
  domanda nuova killa tutti i vecchi processi". Causa reale trovata (non
  piu' un'ipotesi): `core/web_bridge.py::_claim_next_task()` pescava
  SEMPRE il task "pending" piu' VECCHIO (`ORDER BY created_at ASC LIMIT
  1`) dalla coda condivisa `tasks` (mic a mani libere + testo digitato,
  stessa tabella/stesso consumatore seriale) — se il mic accumula piu'
  segmenti mentre uno precedente e' ancora in elaborazione (ognuno puo'
  richiedere secondi, un giro Claude vero), venivano processati e
  RISPOSTI tutti in ordine, anche quando ormai superati dal contesto
  successivo. Non era il problema di continuita' workspace/history
  ipotizzato prima (quello resta un rischio teorico, non confermato/non
  toccato).
- Fix: `_claim_next_task()` ora prende il "pending" piu' RECENTE (`ORDER
  BY created_at DESC`) e marca tutti gli ALTRI "pending" come
  `superseded` invece di lasciarli in coda per dopo — "uccide" letteralmente
  il backlog non ancora iniziato ad ogni nuova domanda, come chiesto. Non
  tocca un task GIA' "running" (il loop e' seriale, non c'e' mai piu' di
  un task davvero in corso). Stesso fix specchiato in `web/api/jarvis.js::
  taskGet()` (route gateway Vercel, verosimilmente non il path realmente
  usato in produzione — vedi nota su web_bridge.py che parla direttamente
  con Turso — ma tenuto allineato per non lasciare due implementazioni
  divergenti). Frontend (`web/public/app.js`): sia `pollTask()` (testo)
  sia `pollTranscribedTask()` (voce) ora riconoscono `status="superseded"`
  e lo mostrano/rimuovono subito invece di aspettare i 200 tentativi e
  mostrare un falso "Timeout: nessuna risposta dal bridge locale".
- Esito: ok, 327/327 test verdi (2 nuovi in `test_web_bridge.py`, JS
  sintatticamente valido via `node --check`, nessun test JS automatico
  esiste per `web/api/`).
- Imparato: una coda FIFO seriale che accumula lavoro piu' veloce di
  quanto riesca a smaltirlo non "si blocca" in modo visibile — continua a
  rispondere, ma a domande ormai irrilevanti, il che dall'esterno sembra
  proprio "si incarta"/"risponde a caso". Promemoria per qualunque futura
  coda simile: FIFO puro va bene solo se il consumo e' piu' veloce
  dell'arrivo, altrimenti serve un criterio esplicito su cosa scartare.
- Prossimo passo: **non verificato dal vivo** (richiede il riavvio del
  bridge + una sessione vocale reale con piu' comandi ravvicinati per
  confermare che il backlog vecchio sparisca invece di essere risposto).
